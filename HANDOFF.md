# 프리케이스 인수인계 — 새 세션용 (2026.08.31 갱신)

새 Claude 세션이 이 프로젝트를 이어받을 때 읽는 문서다. 읽고 나면 "지금까지 뭘 했고, 다음이 뭔지"를 알게 된다.

> ⚠️ **실행 정본은 `SPRINT.md`다.** 이 문서의 아래쪽(0-2 이후)은 **8/15~8/24에 쓴 기록**이고, 그때는 「개발이 끝났고 남은 건 서류뿐」인 상태였다. 그 뒤에 **확장 스프린트(8/28~9/7)**가 통째로 들어왔다 — 실험 4건·신규 기능 7종·공식 측정 런·레드팀. 아래 트랙 표(5장)와 주차 계획은 **그 이전의 계획**이므로 현재 일정으로 읽지 마라.
>
> **지금 무엇을 할 차례인지는 `SPRINT.md`를 보고, 왜 범위에 들어왔는지는 `docs/01-scope.md` 9.1을 봐라.**

---

## 0-1. 지금 어디까지 왔나 (2026.08.31 현재)

**1차 개발은 8/24에 끝났고, 그 뒤 확장 스프린트가 붙었다.** 마감(9/7)까지 시간을 더 쓰기로 한 참가자 결정에 따라 8/28에 R-03\~R-09를 한 번에 편입했고, **범위 편입은 그것으로 마감**했다.

**일정이 계획보다 사흘 앞서 있다. D-4(9/3)까지 계획분이 전부 닫혔다.**

| 축 | 상태 |
|---|---|
| **실험 4건** | **전부 종결 — 채택 1 · 기각 3.** R-09 읽어주기 채택 / R-04 자기일관성·R-04 파생 확신도 루브릭·R-05 약관 주입 기각. **전부 사전 고정 기준이 판정했고, 결과를 본 뒤 기준을 고치지 않았다** |
| **코퍼스** | R-03은 대폭 축소 판정(신규 0건·검증셋 확대 불가). 대신 **결론 불명 28건 전량 회수** → 388건(검색 268 : 검증 120 동결) · DECISION 253 · 쟁점 태그 34종 |
| **공식 측정 런** | 검증셋 120건 × **재현 2회** 완주 (API 실패·형식 오류 0). **이후 판단 계층 동결** |
| **신규 기능 7종** | 유보 이어가기 · 분쟁조정 절차 안내 · 해피콜 연습(S-14) · 가입 전 브리핑 · 근거 각주 · 읽어주기 · 체크리스트 보유 표시. 전부 머지·명세 반영 |
| **안전성** | A5 레드팀 — 인젝션 7/7 · PII 변형 9/9 · 유도 6/6. **실제 결함 1건 발견·수정·세트 편입** |
| **게이트 6종** | 1차 통과 (8/31). **프로덕션 실측은 9/5 배포 후 재확인** |

**공개 수치 (2026.08.31 공식 런 · 화면·문서가 이 값을 쓴다)**

| 지표 | 값 | CI | 회차별 |
|---|---|---|---|
| 결론 구간 정확도 | **92.3% (60/65)** | [83.2, 96.7] | 93.1% · 91.7% |
| 커버리지 | **27.1% (65/240)** | [21.9, 33.0] | 24.2% · 30.0% |
| 두 번 모두 결론이 선 사건 | **25/120 · 96.0% (24/25)** | [80.5, 99.3] | 한쪽만 15건 |
| 유보 구간 정확도 | **64.6% (113/175)** | [57.2, 71.3] | 62.6% · 66.7% |

⚠️ **회차별 값을 반드시 병기한다.** 커버리지는 재 볼 때마다 24\~33%로 흔들리는 값이고, 한 번 잰 값만 걸면 그 흔들림이 수치 뒤에 숨는다. 이전 단일 런(8/17 · 90%·33.3%)은 이 공식 런으로 대체됐다 — 다른 실험이 아니라 **같은 과제의 재측정**이다. 단일 출처는 DB `validation_stats`, 원자료는 `measure/`.

**이 스프린트가 남긴 가장 값진 것 — 기각 실험의 부산물**

커버리지를 올리려는 개입 셋이 전부 기각됐는데, **뒤 둘은 같은 자리에서 무너졌다.** 방법이 전혀 다른 두 개입(확신도 지시문 교체 · 약관 원문 주입)이 틀린 답을 낸 곳은 똑같이 «원래 유보하던 사건을 결론 내게 만든 구간»이었고(신규 커버 76.5% · 66.7%), **원래 결론을 내던 구간은 둘 다 무손상**이었다. 유보되는 사건이 실제로 더 어렵다는 뜻이고, **확신도 임계 4점이 임의의 선이 아니라 난이도 경계에 서 있다는 실측 근거**다. S-06에 공개돼 있다.

**되풀이하지 말 것 — 이번 스프린트의 사고**

| 사고 | 재발 방지 |
|---|---|
| 측정 러너 계량이 **프로덕션 일 예산을 잠가** 신규 상담이 503이었다 | `PRECASE_LIVE=1`이면 계량에서 제외. 측정 지출은 크레딧 잔액으로 따로 본다 |
| lint를 빼먹고 머지해 main CI가 빨갛게 됐다 | PR 전 검사는 **lint · typecheck · test 3종을 함께** 돌린다 |
| CI 머지 게이트 스크립트가 실패를 못 걸렀다 | `gh pr checks N \| awk -F'\t' '$2 != "pass" && $2 != "skipping"'`로 판정. `grep -qv pass`는 오작동한 적 있다 |
| 노션을 머지마다 손으로 고치다 표 행 병합·이스케이프 오타가 났다 | **노션 반영은 9/5 전수 정합 한 번으로 모은다.** 그때까지 정본은 사실상 `docs/`이고, 방향은 **docs → 노션 한 방향** |
| **화면에 박힌 수치가 조용히 낡았다** — 랜딩 코퍼스 배지 360(실제 388) · 처리방침 PII 세트 117(실제 132) · 적재 로더 note 3건 | 상수로 두되 **드리프트를 테스트가 잡게** 한다. 화면은 DB 장애에도 떠야 해서 쿼리로 바꾸지 않았다 |
| 이미 커밋한 docs 위에서 브랜치를 따 **스쿼시에 문서 묶음이 딸려 들어갔다** | docs는 **push한 뒤에** 브랜치를 딴다 |

**남은 일 — 사용자가 직접**

1. **9/5 카카오톡 인앱 실기 3회차** — 신규 기능 8분 (체크리스트 준비돼 있음)
2. ~~**Supabase `app_runtime` 비밀번호 교체**~~ — **컷 (2026.09.01).** 노출 이력이 없고(`.env.local` 미커밋), 봉인 키가 `HKDF(DATABASE_URL + ANTHROPIC_API_KEY)`라 교체하면 **진행 중 세션이 전부 만료되고 전 구간 재실측**이 필요하다. 제출 직전에 넣을 변경이 아니다 — 필요하면 무중단 구간 종료(9/11) 이후
3. **9/6 E-2 대회 지정 양식 + E-5 제출 서류**
4. **9/6 시연 리허설 각본 확정**

---

## 부록 — 1차 개발기(8/15\~8/24) 기록

아래는 확장 스프린트 이전의 기록이다. **현재 일정이 아니라 경위를 읽는 자리**다.

**✅ 2026.08.21 — 채널 슬롯 정의 불일치 발견·수정**

`consult.ts`가 모델에게 `BANCA_HS(은행창구·홈쇼핑)`이라고 알려주고 있었다. 명세(`02-functional.md` F-201)·코퍼스 라벨(`classify_corpus.py`)·화면(`labels.ts`)은 모두 `BANCA_HS(방카슈랑스·홈쇼핑)` · `BRANCH(창구·대면)`인데 **상담 프롬프트만 반대였다.**

시뮬레이터 실주행에서 드러났다 — 「은행 창구에서 ELS 가입」이 `BANCA_HS`로 잡혀 화면에 「방카슈랑스」로 표시됐다. 방카슈랑스는 은행에서 파는 **보험**이라 ELS에 맞지 않는다. 채널은 `search_case`·`analyze_risk_pattern`·`check_documents`의 필터라 **비교군이 통째로 어긋난다.**

수정 후 실호출로 확인: 은행 창구 ELS → `BRANCH` · 은행 저축성 보험 → `BANCA_HS`. 양방향 정확.

**⚠️ 재측정은 필요 없었다 — 앞선 진단이 틀렸다.** 「제품 측정 120건이 이 프롬프트로 돌았다」고 했으나, `measure-product-live.test.ts`는 **`consult`를 호출하지 않는다.** DB의 `facts_summary`를 `judgeRaw`에 직접 넣고 `channel`은 `"UNKNOWN"` 상수다(조사 계층도 태우지 않는다 — 도구 선택 변동을 섞지 않으려는 의도적 격리). 따라서 공개 수치의 전제는 바뀌지 않는다.

**대신 이 사실이 남긴 것** — 공개 수치는 **판단 계층만** 잰 값이었다. 상담 계층의 슬롯 추출 품질은 측정 대상 밖이었고, 이번 결함도 그래서 수치에 잡히지 않았다.

**→ 2026.08.23 측정함 (N-606).** 진술 43건 · 정확 **99.2%(119/120)** CI [95.4, 99.9] · **날조 0/31** CI [0.0, 11.0]. 단서가 아예 없는 진술보다 **다른 것을 가리키는 숫자·장소가 있는** 진술이 위험해서(민원 접수일·갱신일·항의하러 간 은행·현재 나이) 유혹형 10건을 따로 넣었고 전부 비운 채 통과했다.

**⚠️ 이 99.2%는 하한이 아니라 상한에 가깝다** — 진술을 직접 썼고 쓴 사람이 코드 동작을 안다. 조정례 원문은 쓸 수 없었다(완결된 결정서의 서술이지 소비자의 첫 진술이 아니다 — 그 차이가 공개 수치를 갈아엎게 만든 문제였다). 날조 0건도 분모 31이라 CI 상한 11.0%다. 「없다」가 아니라 「이 범위에서 관측되지 않았다」로 읽는다. **실제 진술로 다시 재는 것이 본선 과제다.**

**가드 2종을 붙였다** — ① 소스 대조: 상담 프롬프트와 코퍼스 분류기의 채널 정의가 같은지 검사(문구가 어긋나면 실패) ② 실호출: 모델이 실제로 그렇게 분류하는지 검사. 문구만 맞추고 모델이 다르게 고르면 결함은 그대로이므로 둘 다 필요하다.

**✅ 2026.08.23 — 배포본 전수 점검 + 디자인 정비 (#63·#64)**

노션 사본(docs/) 화면 명세 전체를 배포본과 화면 단위로 대조했다. **필수 구성은 전 화면 충족**이었고 결함 3건이 나와 고쳤다:

| 결함 | 내용 |
|---|---|
| S-05 쟁점 비교 | 문장(judge) vs 태그(case)를 동등 비교해 교집합이 항상 빔 → **설명의무 사건에서 「이 사례에만 있는 쟁점 — 설명의무」라는 거짓 차이** 표시. 「태그 ⊂ 문장」 포함 대조로 수정 |
| S-04 근거 공백 | 같은 실패 문장이 조회 횟수만큼 반복 → 합치고 횟수 병기 |
| S-06 note | 마크다운 `**`가 문자로 노출(2곳) → 재적재 + 로더에 plain text 게이트 |

디자인도 금융 서비스 수준으로 정비(접근성 제약 전부 유지, 새 색은 대비 계산): 브랜드 마크 헤더 · 랜딩 뱃지·진입 카드 아이콘 · 진행 바 · 결론 네이비 배너 · 확신도 도트 · 데모 라벨 sticky 축소. 노션 정본(화면·데이터) 동시 반영 완료.

**✅ 2026.08.24 — 출시 전 점검 (#68·#69)**

카톡 인앱 실기 전에 출시 기준으로 전 화면 재점검. **S-03 필수 구성 「대화 스레드」가 미구현**이었다 — 진술을 보내면 내가 말한 내용이 화면에서 사라졌다. 접이식(「지금까지 말씀하신 내용 보기」)으로 구현(A11Y-4와 절충). 랜딩 진입 카드 가로 배치(두 번째 진입점이 폴드 밖이었음), 진술 placeholder, 나이 라벨 개선. 노션 정본 반영 완료.

**✅ 2026.08.24 — 카톡 인앱 실기 사전 점검 (#73·#74)**

실기에서 걸리면 왕복이 길어지는 지점을 먼저 잡았다. **결함 3건이 나왔고 원인이 같다 — 실패했을 때 되살릴 길을 안 남겨 뒀다.**

| 결함 | 내용 |
|---|---|
| S-04 재시도 버튼 없음 | **EX-302가 요구하는 재시도 버튼이 아예 없었다.** 판단이 끊기면 「상담 다시 시작하기」뿐이라 진술부터 다시 써야 했다 |
| 200이 아니면 세션 전부 파기 | 410(만료)만 실제로 사라진 것인데 429(EX-405)·503(EX-404)까지 버렸다. **잠시 뒤 될 일에 상담을 처음부터** 시키고, 제목도 「상담 내용이 사라졌습니다」로 고정이라 레이트 리밋에 걸린 사람에게 사라졌다고 말했다 |
| 상담 재시도가 `location.reload()` | 동의 화면부터 다시 시작 — **폰에서 4,000자를 다시 쓰라는 뜻.** 상담 한 턴 P95 20초 사이에 앱을 나갔다 오면 걸린다 |

`sessionStorage`가 막히는 인앱 브라우저 대비로 메모리 사본도 뒀다(S-03→S-04는 소프트 내비게이션이라 건넌다). 없으면 상담을 다 마치고 나서야 「이어서 볼 상담이 없습니다」로 끝난다.

**프로덕션 스트림 실측 (카톡 iOS UA)** — 첫 이벤트 **1.6초**(N-102 목표 5초 안), 39바이트 하트비트가 10초 간격에 **오차 0.1초**로 도착, 총 90.3초 14청크 완주.

⚠️ **`Cache-Control: no-transform`이 압축을 막지 못한다** — 응답에 `content-encoding: br`이 붙는다. `no-transform`은 캐시 지시어라 Vercel 엣지 압축에는 효과가 없다. **다만 청크별 flush라 버퍼링은 없었다.** 헤더가 목적을 달성하지 못하는데 결과는 맞는 상태여서, 실측 없이는 안전하다고 말할 수 없었다. `ndjson.ts` 주석의 「압축·버퍼링 방지 헤더를 명시한다」는 절반만 사실이다.

**남은 일 — 세션에서 (2건)**

4. ~~**노션 반영 20건**~~ — **✅ 완료(8/21).** 반영 중 docs/ 자체의 수치 누락 6곳도 함께 정정. 7장 참조
5. **E-5 제출 서류** — E-2가 나와야 시작할 수 있다

**✅ 2026.08.24 — 카카오톡 인앱 실기 통과 (#83·#84) — 배포 게이트 6/6**

실제 아이폰의 카카오톡 인앱 브라우저에서 이동통신망으로 두 바퀴 돌았다.

| | 결과 |
|---|---|
| 1회차 완주 | ✅ 랜딩→동의→진술→되묻기(가입 연월)→판단→**유보**→근거 상세→뒤로가기 전부 정상 |
| 2회차 백그라운드 1분 | ✅ 복귀 시 **「연결이 끊겼습니다」+「다시 시도하기」** → 눌렀더니 **진술을 다시 묻지 않고** 판단 재개 → 결과 도달 |

**⚠️ 실기에서는 iOS가 실제로 연결을 끊는다.** 시뮬레이터에서는 `SIGSTOP`으로 프로세스를 90초 얼려도 스트림이 살아남았는데(네트워크는 그대로였다), 실기에서는 끊겼다. 8/21부터 남아 있던 「시뮬레이터로 증명되지 않는다」가 **끊긴다는 쪽으로 판명**됐다.

**→ 즉, 같은 날 만든 재시도 경로(#74, EX-302)가 없었으면 이 자리에서 떨어졌다.** 원래는 「상담 다시 시작하기」뿐이라 진술부터 다시 써야 했다. 사전 점검이 실기를 통과시킨 셈이다.

**실기에서 이용자가 지적한 UX 2건 — 고쳤다 (#83·#84)**

| 지적 | 무엇이 문제였나 |
|---|---|
| **결과 화면이 길어 「다음에 하실 일」까지 못 내려간다** | 근거 카드가 조문 원문·사건 요지를 통째로 펼쳐 놓아 375px에서 **「다음에 하실 일」이 16.1화면 아래**였다. 거기 F-306의 **신청권 명시와 1332**가 있다 — 화면이 요구를 담고 있어도 닿지 않으면 담지 않은 것과 같다 → 카드를 **자르지 않고 접고**, 제목 아래 **「다음에 하실 일 바로 보기 ↓」** 앵커 추가. **16.1화면 → 12.4화면**, 점프는 한 번 |
| **실행 로그가 코드로 보인다** | `tool lookup_statute · outcome OK · source SNAPSHOT · fallback false`를 그대로 덤프하고 있었다. **S-08 명세가 처음부터 「기술 로그 원문이 아니라 일반 사용자가 읽을 수 있는 문장으로, 도구 원어명은 보조 표기」** 였는데 지키지 않았다 → 도구 5종×결과 4종에 문장, 키·값을 한국어로. **모르는 키는 감추지 않는다**(EP-1) |

**남은 것 — 안드로이드 실기.** 에뮬레이터 Chrome은 쟀으나(첫 이벤트 2.5초·88.9초 완주) 카톡 안드로이드 인앱 자체는 미검증이다. Chrome WebView라 엔진은 같지만 인앱 껍데기 동작은 다를 수 있다. **게이트는 iOS 실기로 닫혔고**, 안드로이드는 있으면 좋은 추가 확인이다.

**✅ 2026.08.24 — 시뮬레이터 전 구간 재주행 (#75·#76·#77·#78·#79·#80)**

카톡 iOS 인앱의 실체인 `WKWebView`를 같은 UA로 띄워(iPhone 17 · iOS 26) **상담 진입부터 결과·근거 상세까지 실제로 눌러 가며** 돌렸다. **결함 2건이 여기서 나왔다.**

| 결함 | 내용 |
|---|---|
| **T-5 위반 — 결과 화면 새로고침이 판단을 말없이 재실행** | 결과가 사라지고 「7초 경과」가 뜨며 90초짜리 판단이 처음부터 다시 돌았다. 모델 호출 한 벌과 **N-203 일일 상한 한 칸**을 설명 없이 쓴다. T-5는 「안내 문구와 함께 복귀, 오류처럼 보이면 안 된다」인데 안내가 없었다 |
| **S-03 가입 연월 — 칸을 누르기만 해도 이번 달이 채워짐** | iOS 휠 피커가 열리며 기본값을 그대로 흘린다. 가입 시점은 기준일(F-303)이고 기준일이 적용 법령을 가르므로(금소법 2021-03-25) **2019년 계약을 2026년 법으로 판단**할 수 있었다. `max` 속성으로 막으려 했으나 **iOS가 `max`를 보지 않아** 코드에서 다시 걸렀다 |

**백그라운드 서스펜드를 강제해 재현했다** — 앱과 WebContent 프로세스를 90초간 `SIGSTOP`으로 얼렸다 풀었다(시뮬레이터는 백그라운드만으로는 정지하지 않는다). **스트림이 살아남아 복귀 직후 결과 화면이 떴다.** 8/21에 「시뮬레이터로는 증명되지 않는다」고 남긴 항목을 여기까지는 좁혔다.

**T-5 수정 방향** — 결과를 클라이언트에 보관하는 길은 택하지 않았다(「창을 닫으면 사라집니다」·개인정보 처리방침). 대신 **진입 경로를 구분**한다: 상담에서 `<Link>`로 넘어온 경우만 자동 실행하고, 새로고침·직접 진입은 「결과는 저장하지 않습니다」로 설명하고 「다시 살펴보기」를 준다. 소프트 내비게이션이 JS 컨텍스트를 유지하는 것을 이용한다.

**그 밖 확인** — 상담·되묻기·판단·결과·근거 상세·뒤로가기·가입 전 확인·검증 결과 전부 정상. PII 마스킹 통과(연락처 포함 진술) · 키보드가 「보내기」를 가리지 않음 · 입력창 글꼴 18.9px로 iOS 자동 확대 미발생 · 핀치 확대 동작.

**안드로이드도 쟀다** — 에뮬레이터(API 36) Chrome에서 판단 스트림 실측: 첫 이벤트 2.5초 · 하트비트 10초 간격 오차 0.2초 · 88.9초 완주 · `content-encoding: br`에도 버퍼링 없음. 카톡 안드로이드 인앱은 Chrome WebView라 같은 Blink 엔진이다.

**남은 것은 실기의 백그라운드 정지 하나다.** `SIGSTOP`은 JS와 타이머를 멈추지만 실기에서는 iOS가 **네트워크 연결까지 끊는다.**

**일정** — 범위 동결 9/3까지 13일. 코드 잔여가 없어 **버퍼가 생겼다**(8/16 시점의 「버퍼 없음」에서 바뀐 부분). 이 버퍼는 양식 지연 흡수용으로 둔다.

**~~임시 자리표시자~~** — 정리 완료(8/16). `coming-soon.tsx` 삭제.

**MCP 규격 어댑터 (`_mcp.ts`) — 만들지 않는다. 종결(2026.08.16)**
`01-scope.md` **SR-X08**이 답을 갖고 있었다: MCP 서버 공개 배포는 **본선 범위**이고 **"1차는 내부 호출만"**이다. 도구 5종을 TypeScript 함수로 두고 ② 조사 계층이 직접 호출하는 현재 구조가 1차 제출의 정답이다.

## 0-2. 실행 명령

```bash
npm test           # vitest 222건 (DB 왕복 포함, .env.local 필요)
npm run typecheck  # next typegen && tsc --noEmit
                   #   ⚠️ typegen이 앞에 있어야 한다 — LayoutProps·PageProps는
                   #      Next가 생성하는 타입이라 .next/types 없이는 tsc가 못 찾는다
npm run lint
npm run build      # 머지 전 필수
npm run dev
```

실주행(모델·법제처 API·DB 실호출)은 기본 실행에서 건너뛴다. 돌리려면:

```bash
PRECASE_LIVE=1 npx vitest run src/lib/agents/__tests__/pipeline-live.test.ts        # 4계층 전 구간(①부터)
PRECASE_LIVE=1 npx vitest run src/lib/agents/__tests__/pipeline-stream-live.test.ts # 판단 스트리밍 ②③④ + 소요 측정
```

⚠️ 실주행 테스트의 `console.log` 표를 보려면 `--disable-console-intercept`를 붙인다. 안 붙이면 vitest가 삼킨다.

---

## 0. 프로젝트 정체

- **프리케이스(PreCase)** — 불완전판매 분쟁 예방·판단 AI 에이전트 웹 서비스
- **2026 금융 AI Challenge** 출품작, 개인 참가 (이용주)
- ⚠️ **주의**: 사용자 계정에 `tour-data-contest-2026` 스킬(관광데이터 공모전용)이 있다. **이 프로젝트와 무관하니 절대 호출하지 말 것.** 마감·양식이 전혀 다르다.

## 1. 핵심 일정

| 날짜 | 내용 |
|---|---|
| **2026.09.03** | 범위 동결 — 이후 범위·수치 변경 금지 |
| **2026.09.06** | 목표 제출 (1일 버퍼) |
| **2026.09.07 10:00** | 제출 마감 |
| 09.07 11:00 ~ 09.11 23:59 | 심사 무중단 구간 — 배포 동결 |

## 2. 자원 위치

**측정 원자료의 위치 (2026.08.26 정리)** — 화면이 공개하는 수치의 근거다.
`measure/`에 요약본, `measure/experiment/`에 산출 코드·실험 결과·데이터셋(코퍼스
라벨 360건 · 검증셋 120건)이 있다. **금감원 조정결정서 원문 HWP 665개만
저장소 밖**이다 — `~/Documents/precase-원자료-hwp-20260826.zip` (공공누리 범위
밖 재배포 방지로 `.gitignore`가 막는다). 자세한 것은 `measure/README.md`.

| 자원 | 위치 |
|---|---|
| **서비스 리포** | `~/precase` (GitHub: yongzooda/precase, private) — Next.js 16 + 마이그레이션 + 스크립트 |
| **개발 규칙** | `~/precase/CLAUDE.md` — **코드 작업 전 필독. 축소 불가 규칙 5종** |
| **명세서 사본** | `~/precase/docs/01-scope.md` ~ `10-db.md` (00-plan.md 기획서만 미export) |
| **데이터 파이프라인** | `~/Downloads/프리케이스` — HWP 원본·corpus.json·labels_v1.json·panel_reasoning.json 등 |
| **노션 (문서 정본)** | 「2026 금융 AI Challenge」 페이지 하위: 기획서 / 통합 요구사항 명세서(9종) / DB 명세서 |
| **배포 (Vercel)** | **프로덕션 URL `https://precase.vercel.app`** — 인증 없이 접근 확인(PR-2 충족). team `yongzooda's projects`(Hobby) / project `precase` / main 자동 배포. 배포별 URL(`precase-<hash>-…`)은 SSO 보호되므로 **외부에 쓰지 말 것** |
| **Supabase** | org `yongju` / project `precase` / Seoul (ap-northeast-2) / Free 플랜 |
| **자격증명** | `~/precase/.env.local` (커밋 안 됨) — DB 롤 2종·Claude API 키. **채팅에 노출 금지** |

작업 시작 시 **폴더 연결 요청**: `/Users/yongju/precase` + (데이터 작업 시) `/Users/yongju/Downloads/프리케이스`. 노션 커넥터도 연결돼 있다.

## 3. 완료된 것 (시간순)

**문서 체계 (완결)**
1. 기획서 정합성 수정 16곳 + 보강 2건(확신도 산출 정의·구 조문 조달 경로)
2. 통합 요구사항 명세서 9종 신규 작성 — 범위→기능→데이터→화면→권한→연동→예외→비기능→용어
3. DB 명세서 작성 (DDL 정본은 리포 migrations)
4. 3자 교차 검토로 결함 31건 발견·전부 반영 (ID 참조·열거·수치·논리 무결성 검사 통과)
5. **미결 사항 U-1~U-11 전부 종결** — 마지막 U-9(쟁점 태그)는 8/15 적재로 확정

**인프라·데이터 (완료)**
6. GitHub 리포 + Next.js 16 스캐폴딩 (`package.json name=precase`)
7. Supabase: 테이블 14 + 뷰 2, 마이그레이션 0001~0005 적용됨
   - 롤 분리: `app_runtime`(정적 자산 SELECT만 — 쓰기 차단 실증 완료) / `batch_loader`
   - 접속: **Supavisor 트랜잭션 모드(포트 6543)**, 사용자명 `롤명.<프로젝트ref>` 형식
   - ⚠️ **세션 모드(5432)를 쓰면 안 된다** — 클라이언트마다 연결을 붙들어 `pool_size 15`에서 막힌다(`EMAXCONNSESSION`). Vercel은 인스턴스마다 풀을 만들어 3~4개만 떠도 한도에 닿는다. Supabase 문서도 서버리스에는 트랜잭션 모드를 권고한다. 2026.08.23 실측으로 확인·교체
8. **코퍼스 360건 적재 (corpus_version=v20260815)** — 검증셋 120 : 검색 240, 겹침 0
   - 라벨 부착률: issues 360 / product_code 360 / channel 116 / traits 35 / decision_no 276 / case_year 301
   - 쟁점·상품군·채널·특성은 모델 분류(claude-sonnet-5), `label_source='MODEL'`
9. **U-9 쟁점 태그 31종 확정** — 상위: 약관해석 195 · 보험금지급범위 171 · 설명의무 64 · 부당권유 52 · 면책사유 48 · 과실상계 37 · 고지의무위반 28 · 적합성원칙 24 · 사기기망취소 23. 전체는 DB 명세서 부록 A
10. `risk_patterns` 사전 집계 294행 (기준 DECISION_225) — `analyze_risk_pattern` 데이터 준비 완료
11. **삭제 조문 스냅샷 6건 적재** — 자본시장법 46·46의2·47·49, 보험업법 95의2(①②항만)·95의3. `statute_timeline`은 A-3에서 적재 완료 — `lookup_statute` 데이터 준비 완결
12. 노션 명세서 10종 → `docs/` export 완료
13. **`statute_timeline` 6행 적재 (A-3, 8/15)** — 금소법 이관 매핑. `lookup_statute` 분기 근거표 완비
14. **`validation_stats` 18행 적재 (A-4, 8/15)** — 원천은 기획서가 아니라 실측 산출물(`results/kpi_report.json`·`reproducibility.json`). S-06 데이터 준비 완료
15. **약관 조항 266건 + 용어 정의 77쌍 적재 (A-1·A-2, 8/15)** — 정규식 전용 추출(모델 0회). 명세의 658/153은 원본 HWP 668건 기준이라 도달 불가 — 실측치로 문서 14곳 정정

**중요 스크립트** (`~/precase/scripts/`)
- `load_corpus.py` — 코퍼스 재적재 (게이트 Q-1·2·5 포함, dry-run 지원)
- `classify_corpus.py` — 라벨 분류 (체크포인트 재개)
- `extract_reasoning.py` — HWP 위원회판단 재추출 (프리케이스 폴더 venv에서 실행)
- `aggregate_patterns.sql` — risk_patterns 재집계 (재적재 후 재실행 필요)
- `load_snapshots.sql` — 삭제 조문 6건 (적재 완료본)

## 4. 미완·주의 사항

**⚠️ 사람이 해야 하는 것 — 금감원 링크 통지 (2026.08.26 발견)**
금감원 저작권정책은 **다른 사이트에서 금감원 저작물을 직접 링크하면 그 사실을
통지**하도록 요구한다. 프리케이스는 모든 조정례 카드에 공식 페이지 링크를 붙이는데
**아직 통지하지 않았다.** `itfss@fss.or.kr` / 02-3145-5376. 공모전 유의사항이
「데이터 무단 사용 시 심사 제외·수상 취소」를 명시하므로, 통지 기록 자체가 성실
이행의 증거가 된다. 마감(09.07) 전에 보내는 것이 좋다.

- ~~정적 자산 미적재~~ — **전 10종 적재 완료(8/15).** `terms_clauses` 266 · `glossary_terms` 77 · `statute_timeline` 6 · `validation_stats` 18
- [x] ~~`docs/00-plan.md` Export~~ — **완료(8/16).** 노션 fetch로 71k자 확보·저장. 이제 `validation_stats`의 `unverified_plan_list` 행을 기획서 10.4 실제 목록으로 교체할 수 있다 (S-06 작업 시)
- ~~Anthropic API 키 재발급~~ — **불필요로 종결(2026.08.15)**. 실제 노출이 없었음을 사용자가 확인. `.env.local` 기존 키를 그대로 사용한다
- 근접 중복 사건 2쌍 존재 (`~/Downloads/프리케이스/data/leakage_report.json`) — 검증 재측정 시 leave-one-out 대상
- `decision_date`는 전부 NULL (원본에 의결일 없음) — 화면 표기는 「의결번호(연도)」 형식
- Supabase Free — **Pro 전환은 필수가 아니다 (2026.08.23 실측으로 재판정)**
  - 용량 **21MB** / 한도 500MB (4%). 저장 금지 설계라 늘지 않는다
  - 정지 조건은 **1주일 무활동**인데 남은 일정의 최장 공백이 **4일**(9/3 동결 → 9/7 심사)이다. 조건에 못 미친다
  - 동시 10세션(N-206) — 프로덕션 `/precheck` 실측 **P95 3.1초**, 목표 10초의 31%. 그중 DB 몫은 0.2초라 병목은 Supabase가 아니라 Vercel 함수 기동이다
  - 백업은 Free에 없지만 **사용자 데이터를 저장하지 않아** 정적 자산 재적재로 복구된다(N-305)
  - → **D-6 감시자를 붙이면 정지 조건 자체가 사라진다.** Pro는 백업·로그 보관을 원할 때의 선택지다. 원자료 `measure/concurrency.md`
- 문서·데이터 수치 변경 시 **동시 갱신 원칙(N-801)**: 노션 정본 + docs/ 사본 + 관련 문서

## 5. 다음 작업 — 1차 제출까지의 전체 계획 (2026.08.15 확정판)

기관 화면 4종·MCP 서버 공개·파일 업로드는 **본선 범위라 여기 없음** (의도된 제외).

### 5.0 확정된 설계 결정

| 항목 | 결정 |
|---|---|
| **MCP 아키텍처** | **Next.js 내 순수 함수.** 별도 서버 프로세스 없음(Vercel 단일 배포 제약). ~~MCP 규격 어댑터~~는 **1차 제출 범위 밖으로 종결(8/16)** — SR-X08이 「1차는 내부 호출만」으로 이미 정하고 있었다. 본선에서 서버로 분리할 때 어댑터를 만든다 |
| **DR-102·103 원천** | `panel_reasoning.json`(위원회 판단 360건). `corpus.json`은 `text`가 [기초사실] 위주라 부적합 — 실측 약관조항 **518건 vs 33건** |
| **추출 방식** | 단일 패스로 두 표 동시 산출 · `sector`/`product_code`는 **`cases` JOIN(재분류 금지)** · 정규식 후보탐지 + 모델 정규화 하이브리드 |
| **DR-106 원천** | `~/Downloads/프리케이스/results/kpi_report.json` + `reproducibility.json` (실측 산출물). 문서 수치 6종 전부 대조 일치 확인 — N-803 정합 |
| **658/153 수치** | **재현 불가.** 기획 단계는 원본 HWP 668건 기준으로 보이고 현재 DB는 360건이다. `terms_clauses.source_case_id`가 `cases(id)`를 참조하므로 대상은 360건으로 묶인다. **실측치를 정본으로 삼고 N-801 동시 갱신 — 9/3 동결 전 완료** |

### 5.1 트랙 A — 데이터 ✅ **완료 (2026.08.15)**

정적 자산 10종이 전부 적재됐다. 백엔드·프론트 트랙의 선행 조건이 풀렸다.

- [x] ~~**A-3** `statute_timeline` 6행~~ — **완료(8/15).** `load_snapshots.sql`에 append. 스냅샷 6건과 `article_no` 1:1 조인 검증(6·6·6). 자본시장법 46→금소법 17 · 46의2→18 · 47→19 · 49→21, 보험업법 95의2(이관항)→19 · 95의3→17. `applies_from`은 전부 null(최초 시행일 미확인 — 추정값 미기입)
- [x] ~~**A-4** `validation_stats`~~ — **완료(8/15).** `scripts/load_validation_stats.py` · 18행 5카테고리. 게이트 2종 통과(Wilson 공식 일치 · 문서 정본 8종 대조). `app_runtime` 읽기 검증 완료
- [x] ~~**A-1+A-2** `terms_clauses` + `glossary_terms`~~ — **완료(8/15).** `scripts/extract_clauses.py` · **약관 조항 266건 + 용어 정의 77쌍(고유 68종)**. 정규식만 사용, 모델 호출 0회. 조항 인용이 `“…”` 안에 원문 그대로 있어 인용 구간을 뜨면 경계가 정확하다(꼬리절단 108 · 접두중복 7 제거). `source_case_id`는 `cases.panel_reasoning` 텍스트 정합 304/304, 불일치 0. F-504 · F-104 · S-07 데이터 준비 완료
- [x] ~~**A-5** 실측 수치 확정 (N-801)~~ — **완료(8/16). docs/ 사본 + 노션 정본 동시 갱신 완결**
  - 신뢰구간 6종(결론구간 [74.2, 89.9] + 업권별 5종)은 **문서 수정 불요** — SR-206·F-403·DR-106·S-06이 전부 CI를 *요구*하지만 값을 고정한 문서는 없다(전수 확인 완료). 명세 이탈이 아니라 이행이다
  - 남는 확인: **기획서 3.3/10.4에 CI가 적혀 있는지 대조** — E-1 Export 후. 값이 다르면 그때만 조정
  - [x] **658 → 266 · 153 → 77쌍(고유 68종)** 으로 `docs/` 14곳 정정 완료 (01-scope 5 · 02-functional 2 · 03-data 2 · 06-integration 2 · 10-db 2 + 열거 2)
  - [x] **낡은 업권 열거 정정** — `10-db.md`가 마이그레이션 0003 미반영 상태였다(`ETC` → `CARD`·`UNKNOWN` 5종). DB는 정상, 문서만 뒤처져 있었음
  - [x] **노션 정본 동시 갱신 완료(8/16)** — 범위 8곳 · 기능 3곳 · 데이터 4곳 · 연동 2곳 · DB 명세서 5곳 · **기획서 4곳** (총 26곳, docs/ 사본과 1:1 대응)
  - [x] **기획서 CI 대조 완료** — 전체 [67.4, 82.6]·합산 [68.7, 79.7]·보험 51~82% 전부 로더 산출과 일치. 결론 구간 CI는 기획서에 없음(모순 아님) → **조정 불요 확정**
  - [x] **신규 발견·정정(8/16)**: ① "정식 결정서 235건" 자산 서술 → 실측 **225건**(stat_basis=DECISION_225) ② "판단 논거 101건(기획서)/235건(명세서)" → 재추출 실측 **304건**(정식 224·요약 80) ③ DB 명세서 "업권 4" → 5. **측정 역사 서술(기획서 10.1 모집단 등)은 보존** — 당시 측정의 사실 기록이라 고치면 안 된다

게이트: Q-1(누출 — `cases.is_validation` 조인 확인) · Q-3(수치 단일 출처) · Q-4(집계 사전 계산) · Q-5(열거 무결성)

### 5.2 트랙 B — 백엔드

**B-1 공통 기반 ✅ 완료 (8/16)**
- [x] 의존성: `@anthropic-ai/sdk`·`postgres`·`zod`·`server-only` + dev `vitest`. 스크립트 `npm test`·`npm run typecheck`
- [x] `src/lib/env.ts` — 설정 12종 + 자격증명 5종, zod 검증·**기본값 없음**(누락 = 부팅 실패로 즉시 발견). `server-only`로 클라이언트 번들 유입 차단(EC-4). **BATCH_DATABASE_URL 필드 자체가 없다**(P-502)
- [x] `src/lib/db.ts` — `app_runtime` 전용 postgres 싱글턴 (max 5 · **트랜잭션 모드 + `prepare: false`**)
- [x] `src/lib/types.ts` — DB 열거 12종 + 슬롯 열거·`SlotsSchema`(F-605)·`deriveTraits`(ELDER 파생). **쟁점 31종은 코드에 안 박는다** — 정본이 `issue_tags` 룩업(D-1)
- [x] 테스트 29개 통과 — 슬롯 검증 10 + **DB CHECK 제약 대조 19**(types.ts↔DB 열거 17쌍 + issue_tags 존재 + stat_basis=DECISION_225). 문서가 낡아 게이트가 오작동했던 사고의 재발 방지 장치

**B-2 MCP 도구 5종 ✅ 완료 (8/16)** — 명세 `docs/02-functional.md` F-501~505
```
src/lib/tools/
  search_case.ts           DB · pg_trgm + 구조 필터 · 인용은 citable_cases 뷰만 · 의결번호 필수
  analyze_risk_pattern.ts  DB · risk_patterns 읽기만 (런타임 계산 = F-603 위반)
  check_documents.ts       DB · terms_clauses 색인 ← A-1 대기
  lookup_statute.ts        외부 · 분기는 조문 종속(기준일 단독 아님) · 캐시 만료행 삭제 금지
  search_precedent.ts      외부 · 사건번호 정확 대조 (캐시 적중에도 항상 수행)
  filter.ts       F-606 명령문 필터 — 법제처 응답도 예외 없음 (EC-2)
  budget.ts       F-607 상한 (사이클 12 / 세션 30)
  law_client.ts   법제처 공통 클라이언트 — Referer 헤더 강제(D-2 참조)
```
⚠️ 계획에 있던 `_mcp.ts`(MCP 스키마 어댑터)는 **만들지 않았다.** 0-1의 「미구현으로 남은 설계 항목」 참조.
외부 2종: 타임아웃 10초 · 멱등 조회 1회 재시도(EC-1) · 출처·확인시점 동반 반환(EC-3) · 폴백은 폴백으로 표시
- [x] ☑ ~~**완료 판정: 판례 정확 대조 성공률 측정**~~ — **완료(8/16). 80.0%(24/30).** 1순위 비율은 16.7%뿐(중앙 순위 4·최대 16)이라 **1순위만 봤다면 13.3%** — 전량 스캔이 **+66.7%p** 회복. 미발견 6건 중 4건은 법제처 DB에 결과 0건(원천 한계, 도구 결함 아님)
- **⚠️ E-02 결함의 성격이 정정됐다(8/16 재조사)** — 「반환되지 않는다」가 아니라 **「1순위로 오지 않는다」**였다. 요청 사건번호는 결과 목록 안에 실제로 있다. 실측 3건 전부 목록 끝에서 발견: `2010다76368` 16/16 · `2016다212272` 5/5 · `2019다224238` 4/4.
  → **F-502는 1순위가 아니라 `display=100`으로 결과 전량을 스캔해 정확 일치를 찾는다.** 사건번호 조회는 4~16건 규모라 1회 호출로 충분. **끝 항목을 집는 편법은 쓰지 말 것**(위치 보장 없음) — 전량 스캔이 규칙이다. 이 정정으로 **판례 근거 확보율이 크게 오른다**(연동 7장 체크 3 측정 대상)

**B-3 에이전트 4계층 ✅ 완료 (8/16)** — 명세 F-301~309
```
src/lib/agents/
  consult.ts      ① 원문 읽기 O · 슬롯 쓰기 · 되묻기 1문1답 (8/10 상한)
  investigate.ts  ② 슬롯만 · 도구 오케스트레이션 · contract_ym → 기준일 산정
  judge.ts        ③ JudgeInput에 rawStatement 필드 부재 — 타입 강제
  guide.ts        ④ 판단 결과 읽기 전용
  model.ts        Claude 호출 공통층 (구조화 출력 / 스트리밍)
  types.ts        계층별 페이로드 타입 — 권한 매트릭스의 구조적 구현체
  slots.ts        필수 5 + 조건부 3(발동 시 필수 승격)
  pii.ts          F-601 마스킹 — 모델 전송 전 · 잔존 의심 시 전송 차단(EX-106)
  session.ts      메모리 전용 · 30분 유휴 파기
  trace.ts        F-402 append 전용 · 세션 내 열람
```
핵심 3: **판단 페이로드에 원문 필드 자체가 없을 것** · 확신도 임계 4 미만 유보(설정값) · 되돌림(F-308)

- [x] `model.ts` — Claude 호출 공통층. `callStructured`(구조화 출력, 형식 위반 → EX-303 유보) / `callText`(스트리밍, N-102). `temperature`·`top_p` 미사용(운영 모델에서 제거된 파라미터)
- [x] `consult.ts` ① — 슬롯 추출·범위 판정(F-604)·되묻기 1문1답. 병합은 반드시 `applySlots` 경유(F-605 열거 차단 + ELDER 파생). 모델이 비운 값은 「모른다」이지 UNKNOWN 응답이 아니므로 슬롯에 넣지 않는다
- [x] `investigate.ts` ② — **도구·순서 모델 자율 선택**(F-303), 자율 범위를 3겹으로 제한: 법령명 허용목록 7종 · 호출마다 `ToolBudget` 소진 · **근거는 도구 반환값에서만 축적**(모델 텍스트를 읽지 않는 것이 F-603의 구조적 구현). 금소법 전후 판정은 프롬프트에 **사실로 박아** 넣는다 — 모델이 시점을 추론하면 오적용이 되살아난다
- [x] `judge.ts` ③ — `JudgeInput`에 원문 필드 부재(타입 강제) + **`evidence.cases`도 프롬프트에 넣지 않는다**(기획서 6.5 측정 결과). `_judgeSeesNoCases` 컴파일 단언 + 완성 프롬프트 문자열 검사 테스트 양쪽으로 막았다. 임계는 `env.CONFIDENCE_THRESHOLD`에서만 읽는다
- [x] `guide.ts` ④ — **F-306 필수 4항목을 모델에 맡기지 않는다.** ①신청권·②1332는 코드가 붙이고, ④조건은 판단 결과 그대로 통과, ③만류 표현은 `stripDiscouraging`이 문장 단위로 제거하고 실행 로그에 남긴다
- [x] 테스트 127통과 (신규 layers 27 + 기존 100). 만류 표현 9종 차단 · 정상 문장 5종 보존 검증
- [x] **실주행 2건 통과** (`pipeline-live.test.ts`, `PRECASE_LIVE=1`에서만 실행) — 단위 테스트가 통과해도 계층 접합부는 따로 깨진다는 걸 되묻기 결함에서 겪었으므로 시나리오 전 구간을 태우는 경로를 남겼다

**실주행에서 확인된 것 (2026.08.16, TM·67세·INS_WHOLE·2019-05·설명의무)**
- **시점별 법령 적용이 실제로 작동한다.** 2019년 사건에 보험업법 제95조의2·95조의3 스냅샷을 조회하고 **금소법을 인용하지 않았다.** 모델이 상법 제638조의3(보험약관 교부·설명의무)·약관규제법 제3조·제5조까지 스스로 추가 — 자율 선택이 의도대로 동작
- 도구 8~9회 소진(사이클 상한 12), 결과 유보(확신도 3). 커버리지 71% 문서값과 어긋나지 않는다
- **결함 2건 발견·수정**: (a) `search_case` 필터 전부 AND라 `INS_WHOLE×TM×ELDER×설명의무`가 0건 → 조사 계층에 폴백 사다리 추가(3건 확보, 완화 사실은 `gaps`로 화면에 노출) (b) 유보 시 `needed` 6항목이 한 줄로 뭉쳐 나옴 → `GuideResult.needed` 배열로 분리(고령 이용자 가독성)

예외 EX-101~106 / 201~207 / 301~304 전부 구현. EP-3대로 **실패는 안전한 방향으로**.

**B-4 운영 API ✅ 완료 (8/16)**
```
src/lib/ops/
  counters.ts   DR-302 익명 카운터 — 이벤트 이름 하나만 받는다
  reports.ts    DR-301 신고 접수 — 마스킹 게이트 통과분만 INSERT
  health.ts     N-302 의존성 프로브 3종 + 등급 산정(순수 함수)
  http.ts       운영 API 공통 응답 규약 (S-03·S-04가 물려받는다)
src/app/api/
  health/route.ts  GET   · report/route.ts  POST · event/route.ts  POST
```
- [x] **신고(F-404)** — 마스킹 통과분만 INSERT. **잔존 의심이면 DB에 닿지 않고 400으로 되돌린다**(신고를 받는 것보다 개인정보를 남기지 않는 쪽이 무겁다 — EP-3). 세션·IP·UA는 받지 않는다(넣을 컬럼 자체가 없다 — P-604)
- [x] **카운터(DR-302)** — 일 단위 UPSERT, **날짜 경계는 DB 시각 기준 KST**(리전이 바뀌어도 값이 흔들리지 않게). **클라이언트가 올릴 수 있는 이벤트는 2종뿐**(`RECONSULT_ENTRY`·`DEMO_VIEWED`) — 브라우저가 `JUDGMENT_DONE`을 올릴 수 있으면 KPI가 측정값이 아니게 된다. 실패는 삼키고 사용자 흐름을 막지 않는다
- [x] **헬스체크(N-302)** — 실호출 확인: DB 306ms · 법제처 593ms · Claude 429ms. **Claude 프로브는 `models.list`라 토큰을 쓰지 않는다**(N-204 쿼터 무관). 명세 3종 중 정적 화면·데모는 감시자 몫으로 분담 — `08-nfr.md` N-302에 기록
- [x] 열거는 `types.ts`의 `EVENT_TYPES` 재사용 — 복제하면 DB CHECK 대조(enums-db)를 받지 않는 두 번째 정의가 생긴다
- [x] 테스트 141통과 (신규 ops 14 + 기존 127)

**⚠️ 실측으로 확정된 권한 제약 2건** — `10-db.md` 8장에 기록
- **`error_reports`에 `insert ... returning`을 못 쓴다.** PostgreSQL이 RETURNING 컬럼에 SELECT 권한을 요구하는데 `app_runtime`은 INSERT만 가진다. 권한을 넓히는 대신 **접수번호를 앱에서 만들어 INSERT**한다. `select ... from error_reports`가 거부되는 것을 테스트로 고정했다
- **`error_reports.channel` CHECK에 `UNKNOWN`이 없다.** 슬롯 열거는 UNKNOWN을 포함하므로 화면 값이 그대로 오면 신고가 통째로 실패한다 → 접수 계층에서 null로 접고 접었다는 사실을 응답에 실어 알린다

**⚠️ 쓰기 테스트는 반드시 트랜잭션 롤백으로** — 테스트가 치는 DB는 운영 DB이고 `app_runtime`에 DELETE 권한이 없다(설계 의도). 커밋하면 KPI 카운터와 신고 목록이 테스트 값으로 오염되고 **지울 방법이 없다.** 같은 이유로 라우트 스모크 테스트도 **거절 경로만** 실호출했다

### 5.3 트랙 C — 프론트엔드 8화면

**8개 중 3개는 에이전트가 필요 없다. 그것부터 만들어 배포를 미리 태운다.**

| 화면 | 모델 | 선행 | 배치 |
|---|---|---|---|
| S-01 랜딩 | ❌ | — | 1주차 |
| S-06 검증 결과 | ❌ 정적 | A-4 | **1주차 · 축소 불가** |
| S-02 사전 점검 ✅ | ❌ **모델 미사용**(F-201~203) | 도구 2종 | **완료 8/16** |
| S-05 근거 상세 ✅ | ❌ **모델 미사용** | B-2 | **완료 8/16** |
| S-03 상담 대화 ✅ | ✅ | B-3 ① | **완료 8/16** |
| S-04 판단 결과 ✅ | ✅ | B-3 전체 | **완료 8/16** |
| S-07·S-08 ✅ | 조건부 | **S-04 흡수 완료** | **완료 8/16** |

**S-05 근거 상세 ✅ 완료 (8/16) — 화면 트랙 완결**
```
src/app/judgment/compare.ts          공통점·차이점 계산 + 요지 발췌 (순수 함수)
src/app/judgment/evidence-detail.tsx 조정례 / 법령 / 판례 상세
```
- [x] **공통점·차이점에 모델을 쓰지 않는다** — 확정 슬롯과 `SimilarCase` 필드를 축별로 맞대볼 뿐이라 지어낼 여지가 없다. F-603을 프롬프트가 아니라 구조로 지키는 또 하나의 자리
- [x] **빈 라벨을 일치로 읽지 않는다** — 조정례 채널 부착률이 116/360이라, 없는 값을 같다고 읽으면 「가입 경로가 같습니다」라는 거짓말이 나간다. 「견줄 수 없는 항목」으로 따로 밝힌다
- [x] **유사도 점수·백분율을 만들지 않는다** — 도구가 준 적 없는 수치이고 이용자는 승산으로 읽는다
- [x] ⚠️ **조정례 요지는 발췌한다 — 작업 중 발견한 문제** — `factsSummary`가 실측 **1,258~15,956자**(중앙값 5,007)다. 초안이 이걸 통째로 렌더하면서 바로 아래 "원문 전문은 싣지 않습니다"라고 적고 있었다. DR-101 위반이자 자기모순. 문장 경계 발췌(S-04 320자 · S-05 700자) + 잘랐다는 사실 명시로 고쳤다
- [x] **배상비율은 S-05에서만** — 병기할 자리가 있어 SR-X10을 충족한다(S-04 카드에서는 여전히 미표시)
- [x] **독립 route가 아니라 S-04 안의 전이 상태** — 근거가 판단 스트림 직후의 클라이언트 메모리에만 있어, 새 페이지로 열면 판단을 다시 돌리는 수밖에 없다(100초·모델 비용). `history.pushState`로 **뒤로가기 = T-3 복귀**를 구현했다
- [x] **한국어 조사를 받침으로 고른다** — 「가입 경로이(가)」 같은 병기 표기가 고령 이용자에게 껄끄럽다. 규칙으로 정확히 고를 수 있는 것을 대충 두지 않았다
- [x] 실데이터 확인 — "가입 경로가 같습니다. 다만 상품은 다릅니다." / "상품 — 내 사건은 종신보험, 이 사례는 기타 보험" / 뒤로가기 복귀 동작

**S-04 판단 결과 ✅ 완료 (8/16) — S-07·S-08 흡수**
```
src/app/judgment/judgment-result.tsx   결론 A / 유보 B 상태
src/app/judgment/evidence-views.tsx    근거 3분류 — 도구 반환 객체만 props (F-603)
src/app/judgment/result-views.tsx      결론·확신도·쟁점·다음단계·유보사유
src/app/judgment/execution-log.tsx     실행 로그 접이식 (S-08 흡수)
src/app/judgment/report-form.tsx       오류 신고 (F-404 → B-4의 /api/report)
```
- [x] **`done` 이벤트를 넓혔다** — 근거·슬롯·실행 로그 전문을 싣는다. 문자열로 요약해 보내면 화면이 그 문자열을 믿게 되고 F-603이 깨진다. **근거는 도구 반환 객체 자체로 건너간다**
- [x] **F-306 「한 벌」을 렌더 테스트로 고정** — `react-dom/server`로 실제 HTML을 만들어 결론·확신도·근거 3분류·신청권·1332·판단이 달라지는 조건이 다 나오는지 검사한다. **새 의존성 없이 된다.** 섹션 하나가 빠진 화면이 조용히 통과할 수 없다
- [x] **배상비율은 표시하지 않는다** — SR-X10이 "참고 사례 n=N 기준, 예측치 아님" 병기를 요구하는데 조정례가 카드로 쌓이는 구조라 카드마다 붙이면 안 읽히고 안 붙이면 예측으로 읽힌다. **병기 불가 시 표시 금지**를 택했고 테스트로 고정했다
- [x] **확신도를 백분율로 바꾸지 않는다** — 「이길 가능성을 나타내는 값이 아니며」를 명시. 테스트가 `\d+%` 패턴을 막는다
- [x] **유보에도 ④ 판단이 달라지는 조건** — `guideWithheld`는 `changesIf`가 비어 있어 그 자리를 「이 자료가 있으면 판단할 수 있습니다」가 맡는다. 안내 문장에서 연결을 명시했다("아래 자료가 확인되면 판단이 달라질 수 있습니다")
- [x] **실패를 감추지 않는다** — 실행 로그가 ⚠와 사유를 그대로 싣는다(실측 16단계). 근거를 못 구했으면 "없는 근거를 지어내지 않습니다"라고 적는다
- [x] **실데이터 확인** — 상담 39초 → 판단 완료. 보험업법 95의2·95의3 스냅샷 + 금소법 이관 안내 + 상법 638조의3 + 약관규제법 3·5조가 근거로 렌더됨. 유보 경로 필수 자료 7항목
- ⚠️ **S-05 진입 링크는 아직 없다** — 근거 항목에서 S-05로 들어가는 경로는 S-05 구현 때 붙인다(없는 화면으로 링크를 먼저 두지 않는다)

**S-03 상담 대화 ✅ 완료 (8/16)**
```
src/lib/agents/statement.ts        ① 상담 전용 진술 보관 — 세션 객체에 두지 않는다
src/app/api/consult/route.ts       동의 · 진술 · 되묻기 한 턴
src/app/consult/                   동의(F-602) → 진술(F-301) → 되묻기(F-302) → 완료
src/app/judgment/                  판단 스트림 진행 표시 (S-03→S-04 전이 구간)
```
- [x] **진술을 `Session`에 넣지 않았다** — `runJudgment(session)`이 세션을 통째로 받으므로, 원문을 세션에 올리면 판단 계층 코드에서 손이 닿는다. 별도 모듈에 두면 **import하지 않는 계층은 접근할 방법 자체가 없다**(SR-402를 「아무도 안 읽는다」가 아니라 「닿을 수 없다」로 유지)
- [x] **답변은 반드시 `applySlots` 경유** — 클라이언트 값이 세션 슬롯에 직접 들어가는 경로를 만들지 않았다(F-605 + ELDER 파생)
- [x] **모든 질문에 「모른다」 경로** — 선택지형은 UNKNOWN 항목, 숫자·연월은 「기억나지 않습니다」 버튼. 없으면 이용자가 아무거나 고르고 그게 틀린 슬롯이 된다. 테스트로 고정
- [x] **세션 id는 `sessionStorage`로 나른다** — URL에 두면 주소창·기록·공유 링크에 열쇠가 남는다
- [x] **실호출 전 구간** — 동의 → 진술 → 되묻기 4턴 → 완료 → 판단. 상담 **36.5초**(턴당 5~9초, N-102 목표 안) · 판단 **82초**. 조건부 슬롯 `confirm_call`이 보험+설명의무 쟁점에서 자동 승격되는 것까지 확인
- [x] 안전 경로 — 잔존 PII 차단 · 범위 밖 거절 · 동의 없는 진술 차단
- ⚠️ **판단 결과는 아직 렌더하지 않는다** — F-306이 한 벌을 요구하므로 일부만 내면 그 자체로 위반이다. `/judgment`는 진행과 완료 사실까지만 보인다
- [x] ~~**세션이 Vercel 인스턴스 교체를 버티는지 — 프로덕션 실측(8/16)**~~ — **이 결론은 틀렸다. 아래 참조**

**⚠️ 세션 구조 — 서버 메모리에서 봉인 토큰으로 (8/16 정정)**

처음엔 세션을 서버 프로세스 메모리 `Map`에 뒀고, 프로덕션 측정에서 「유지된다」고 결론냈다(연속 5요청 44.8초 · 60초 공백 210초). **그 측정이 전부 `/api/consult` 단독이었다** — 같은 함수 안에서만 확인한 셈이다.

S-04를 붙이자 드러났다:
```
1) /api/consult  같은 세션 → 200
2) /api/judge    같은 세션 → 410
3) /api/consult  같은 세션 → 200   ← 세션은 살아 있다
```
**Vercel은 라우트마다 별도 함수로 배포해 메모리를 공유하지 않는다.** 로컬 `next dev`는 단일 프로세스라 끝까지 통과한다 — **로컬 통과를 근거로 분산 환경 동작을 판단한 것이 오류였다.**

**해결** — 세션을 **AES-256-GCM으로 봉인해 클라이언트가 들고 다닌다**(`src/lib/agents/seal.ts`). DB·KV는 절대 규칙 3(DR-4xx 저장 금지)에 걸려 못 쓴다. 서버에는 아무것도 남지 않고 브라우저는 열어볼 수 없다. 키는 기존 서버 비밀에서 HKDF 파생(새 환경변수를 요구하면 값을 넣기 전까지 배포가 죽는다).
- 진술은 봉인 안에 있지만 **`Session` 객체로 올라오지 않는다** — `openSession()`이 따로 돌려주고 상담 라우트만 쓴다. SR-402 유지
- **도구 상한 카운터·유휴 만료 시각도 봉인 안** — 클라이언트가 되돌릴 수 없다
- **프로덕션 전 구간 확인(8/16)**: 상담 30초(되묻기 3회) → 판단 98초 → 근거 법령 7·조정례 1 · 실행 로그 17단계 · **상담 구간 로그가 판단까지 이어짐**

**교훈 — 로컬에서만 통과한 것은 「된다」가 아니다.** 라우트를 넘나드는 상태는 프로덕션에서 반드시 실측할 것

**S-04 선행 — 판단 API 스트리밍 골격 ✅ 완료 (8/16)**
```
src/lib/agents/pipeline.ts   ②③④ 스트리밍 생성기 — S-04가 그대로 소비한다
src/lib/ops/ndjson.ts        줄 단위 JSON + 버퍼링 방지 헤더
src/app/api/judge/route.ts   세션 기반. maxDuration 300
```
- [x] **⚠️ 「Vercel 함수 실행 시간 제한에 걸린다」는 서술은 틀렸다** — Hobby는 Fluid compute 기본값으로 `maxDuration` **기본 300초·최대 300초**다(공식 문서 확인). 판단 P95 120초는 한계에 닿지 않는다
- [x] **그래도 스트리밍은 필수다. 이유가 다르다** — Vercel 문서: HTTP/1.1에는 HTTP/2의 PING에 해당하는 프레임이 없어 **중간 네트워크 계층이 유휴 연결을 닫을 수 있으므로 작업 중 진행 데이터를 흘려보내야 한다.** 카카오톡 인앱(N-701) 경로에서 특히 중요하다. N-102(스트리밍 시작 5초)도 그대로 유효
- [x] **파이프라인 실측 2회** — 전체 **88.6초 / 93.2초**. ② 조사 49~51초(도구 9~10회) · ③ 판단 39~42초 · ④ 안내 0.2초(유보는 모델 미호출). **결론 경로**의 `guide()` 모델 호출은 따로 재서 **7.1초** → 합산 최악 **약 100초**(상한의 33%). 첫 이벤트 **1ms**
- [x] **프로덕션 스트리밍 실측** — **120초·240초** 모두 5초 간격 하트비트로 **버퍼링 없이 실시간 도달**(벽시계와 경과시간 일치). 240초도 끊김 없이 완주해 실측 최악 100초의 **2.4배 여유**가 확인됐다
- [x] **SSE가 아니라 NDJSON** — 단방향이라 재연결·이벤트 ID가 불필요하고, 중간 프록시의 SSE 버퍼링 사고를 피한다. `no-transform`·`X-Accel-Buffering: no` 명시
- [x] **판단 본문은 `done`에서 한 번에** — 결론을 토막내 흘리면 이용자가 중간 조각을 결론으로 읽는다(S-03 금지사항 "승산 낙관 표현"과 같은 위험). 스트리밍하는 것은 **진행 상태**지 결론이 아니다
- [x] 창을 닫으면 파이프라인도 멈춘다(스트림 cancel → 생성기 종료). 남은 모델 호출을 태우지 않는다
- ⚠️ **Preview URL은 SSO로 막혀 있다(302 재확인).** 장시간 스트리밍 실측은 프로덕션에서만 가능하다 — 측정용 `/api/judge/probe`는 측정 직후 제거했다

**S-02 사전 점검 ✅ 완료 (8/16)**
```
src/lib/labels.ts             열거 → 한국어 표기 (Record 타입이라 코드가 늘면 빌드가 깨진다)
src/app/precheck/steps.ts     단계 판정 — 순수 함수, URL 질의 문자열이 상태
src/app/precheck/views.tsx    결과 표시 — 도구 반환 객체만 props로 받는다 (F-603)
src/app/precheck/page.tsx     3문항 + 결과
```
- [x] **모델 호출 0회** — 구조화 폼 + 도구 2종 반환값 렌더링만. Claude가 죽어도 이 화면은 산다(EX-401)
- [x] **상태를 URL로 들고 간다** — 링크·GET 폼만 쓰므로 **스크립트 없이 전 단계가 굴러가고**, 뒤로가기가 「이전 질문」이 된다(고령 이용자에게 가장 익숙한 되돌리기). 카카오톡 인앱(N-701) 위험도 줄어든다
- [x] **질의 문자열은 전부 열거 대조** — 이용자가 직접 고칠 수 있으므로 열거 밖 값은 무시하고 그 질문으로 되돌린다. 나이 형식 오류는 **조용히 UNKNOWN으로 접지 않고** 다시 묻되 **입력값은 되돌려 준다**
- [x] **물러나 읽은 사실을 감추지 않는다** — `analyze_risk_pattern`의 폴백 사다리 때문에 「데이터 없음」보다 **「더 넓은 기준으로 집계」**가 훨씬 자주 발생한다(종신보험×TM×60세이상 → 종신보험). `fellBack`·`matched`를 화면에 그대로 띄운다. `04-screen.md` S-02에 기록
- [x] **약관 조항은 렌더하지 않는다** — `check_documents`가 조항을 **쟁점만으로** 뽑고 상품군으로 좁히지 않아서(쿼리 실측), 상품을 방금 고른 화면에 두면 그 상품 약관으로 읽힌다. **S-07의 몫**
- [x] 테스트 18건 · 실호출 확인(3문항 전 단계 + 결과 2종 + 오류 상태) · 터치 영역 48px 미만 0개 · 본문 18px
- ⚠️ **결과 화면 문구는 상품군 중립이어야 한다** — 초안이 "보험금이 거절되었거나"라고 써서 펀드 결과에서 어색했다. 13개 상품군 어디에 와도 맞는 말인지 확인할 것

공통 규약:
- **환각 방지는 렌더링 계층에서 구조적 강제** — 근거 컴포넌트는 도구 반환 객체만 props로 받고 문자열을 받지 않는다 (F-603)
- 「가능성 낮음」·유보에 ① 신청권 명시 ② 1332 안내 ③ 말리는 표현 금지 ④ 판단이 달라지는 조건 — **4개 전부** (F-306)
- 접근성: 18px · 48×48px · 4.5:1 · **한 화면 하나의 질문** · 열거형은 선택지 버튼("모름" 상시) · 카카오톡 인앱
- 폴백 배지(EP-1) · 사용자 책임 화법 금지(EP-5) · 세션 유휴 30분 만료(EX-407)
- ⚠️ **판단 P95 120초** → `maxDuration` + 스트리밍을 **S-04 착수 시점부터**. 나중에 얹는 구조가 아니다

### 5.4 트랙 D — 배포 인프라

- [x] ~~**D-1** Vercel 프로젝트 생성·GitHub 연결~~ — **완료(8/16).** `precase.vercel.app` 배포 성공(커밋 e493b33). 환경변수 15종 이전, **`BATCH_DATABASE_URL` 제외 확인(P-502)** · 미사용 `NEXT_PUBLIC_SUPABASE_URL` 제거. main push = 자동 프로덕션 배포, PR = Preview URL 자동 발급
- [x] ~~**D-2** 법제처 도메인 등록~~ — **완료·실호출 검증까지(8/16).** 등록 위치는 마이페이지가 아니라 **[OPEN API] → [OPEN API 신청]**이었다. `precase.vercel.app` 등록 · 법령종류 체크(현행법령·판례·법령해석례 × 목록/본문 JSON) · **자동승인**(승인 지연 리스크 소멸). **⚠️ 최대 발견: 법제처는 `Referer` 헤더로 검증한다** — 모든 호출에 `Referer: https://precase.vercel.app/` 필수. 없으면 `"필수입력요소 검증에 실패"`라는 **엉뚱한 에러**가 온다(파라미터 문제로 보이지만 원인은 Referer 부재). IP 검증이 아니라 Vercel 동적 IP 무관. **배포 도메인 변경 시 신청 정보도 함께 변경 필요**
- [ ] **D-3** IP 레이트 리밋 미들웨어 (N-203: 분당 30·세션 시간당 5·판단 일 10 — 휘발 메모리만)
- [ ] **D-4** Claude API 하드 쿼터(N-204) · 헬스체크 5분 + 운영자 알림(N-302·303)
- [x] ~~**D-6 감시자 설정**~~ — **완료(8/23).** UptimeRobot 무료, 모니터 3종(`/api/health?strict=1` · `/` · `/demo`) 전부 5분 주기·Up. 알림 도달 확인. 유료 기능(키워드 감시·1분 주기·HTTP 메서드)에 기대지 않도록 `?strict=1`을 새로 만들었다. 부수 효과로 Supabase 정지 조건 소멸
- [ ] ~~**D-5** Supabase **Pro 전환**~~ → **선택 항목으로 강등 (2026.08.23 실측)**. 아래 근거 참조

### 5.5 트랙 E — 문서·제출

- [ ] ⚠️ **E-2 대회 지정 양식 실물 확보** (기획서·기능설명서 PDF) — **8월 중 필수. 구현과 별개 트랙이고 현재 최대 불확실 요소**
- [ ] E-1 `docs/00-plan.md` 노션 수동 Export (미검증 항목 산문용)
- [ ] E-3 HANDOFF.md 수치·상태 동기화 (N-801) — 상시
- [ ] E-4 개인정보 처리방침 푸터 (N-504)
- [ ] E-5 제출 서류 작성 + 데모 URL (9/1~9/5)

### 5.6 트랙 F — 하드닝·측정

- [x] ~~**PII 마스킹 합성 100건 — 미검출 0건**(N-501)~~ — **✅ 완료(8/17). 117건 · 미검출 0 · 정상 문장 훼손 0**
  - 최초 측정 **미검출 28건**(전부 성명, 5유형: 라벨+조사 · 라벨 누락 · 자기소개 · 역할 라벨 · **라벨 없는 맨이름**)
  - 맨이름은 성씨로만 잡히므로 조건을 3겹으로 조였다 — **어절 시작 · 성씨+정확히 2음절 · 조사 필수**. 느슨하면 참사가 난다(실측: 종**신보험**·해**지환급금**이 이름으로 잡혔고, 조사를 선택으로 두자 「전화로」·「원금이」가 통째로 이름이 됐다)
  - ⚠️ **과잉 마스킹이 늘 안전하지는 않다** — 초기 규칙이 「어머니가」를 지웠는데 대리 가입 여부는 판단에 직결되는 사실이다. 가족 호칭·흔한 명사를 예외에 넣고 **정상 문장 훼손 0건**을 테스트로 고정했다
- [x] ~~서버 로그 전수 점검(N-403)~~ — **✅ 완료(8/17). 위반 1건 발견·수정**
  - `investigate.ts`가 도구 실패 시 **오류 메시지 원문**을 실행 로그 detail에 싣고 모델에도 돌려주고 있었다. postgres는 파라미터 값을 메시지에 담으므로 **슬롯 값이 새는 경로**였다. 분류만 남기도록 수정
  - 점검을 **테스트로 고정** — 사람이 훑는 점검은 다음 커밋에서 무너진다. 승인 목록은 줄 내용까지 고정하고 사유를 적게 했다. 가드가 실제로 잡는지 결함을 되돌려 넣어 확인
- [x] ~~환각 자동 대조 테스트 CI 포함(N-405)~~ — **✅ 완료(8/17). 구멍 1개 발견·수정**
  - **`guide.explanation`은 모델 산문인데 화면이 그대로 렌더하고 있었다** — 모델이 「민법 제750조에 따라…」를 쓰면 도구가 반환한 적 없는 조문이 화면에 나가는 경로. CLAUDE.md가 금지한 `<div>{llmOutput.statuteText}</div>` 그 형태다
  - **④ 안내 계층 + 렌더 계층 두 곳**에서 대조한다. ①만 두면 「모델 출력을 거른다」는 약속이고, ②가 있어야 「화면에 나올 수 없다」는 구조가 된다
  - GitHub Actions PR 체크 도입 — **배포 게이트 3종이 상시 검증**된다
- [ ] Lighthouse 접근성 90+(N-703) · 카카오톡 인앱 실기 — **기기·실행 필요**
- [ ] 정확도·커버리지 재측정(N-601·602 — 근접 사건 2쌍은 leave-one-out)
- [ ] **데모 3종**(예방 A·판단-결론 B·판단-유보 D) — DB 아닌 **배포 번들 정적 자산**(DR-107), "데모 사례" 라벨

### 5.7 주차별 실행 순서 (8/15 원안 — **실제 진척은 0-1 참조**)

| 주 | 트랙 | 완료 판정 |
|---|---|---|
| **8/16~8/22** | A 전체 · B-1 · **D-1·D-2** · S-01·S-06 | **배포 URL이 살아 있고 S-06이 실측 수치를 렌더** · 법제처 도메인 등록 신청 완료 |
| **8/23~8/29** | B-2 도구 5종 · S-02 · F 마스킹 예비 | 판례 대조 성공률 측정 완료 · **예방 축이 모델 없이 동작** |
| **8/30~9/5** | B-3·B-4 · S-03·04·05·07·08 · D-3~5 | **9/3 범위 동결** · 판단 파이프라인 E2E |
| **9/6~9/7** | F 하드닝 · E-5 | **9/6 제출** (마감 9/7 10:00, 1일 버퍼) |

**의존성 임계 경로**
```
B-1 ─→ B-2 ─→ B-3 ─→ S-03/04/05     ← 가장 긴 사슬
A-1 ─→ B-2 check_documents ─→ S-02
A-4 ─→ S-06                          (독립 · 1주차 완료)
D-1 ─→ D-2                           (외부 승인 대기 가능)
```

### 5.8 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **에이전트가 마지막 주로 밀림** | 최대 — 임계 경로 끝단 | S-07·S-08을 처음부터 S-04 흡수로 확정해 화면 2개 제거 · A-1을 부분 추출로 조기 종료 |
| ~~판단 파이프라인이 Vercel 함수 제한에 걸림~~ | — | **해소(8/16)** — 상한은 300초, 실측 최악 100초. 스트리밍 골격도 프로덕션에서 120초 검증 완료 |
| **대회 양식 미확보(E-2)** | 구현과 무관한데 작성 분량이 좌우됨 | 8월 셋째 주 내 확인 |
| ~~법제처 도메인 승인 지연~~ | — | **해소(8/16)** — 자동승인이었고 3종 실호출 검증 완료 |

### 완료 판정
트랙 A~F가 전부 체크되면 1차 제출 완료다. 이후는 심사 대기(무중단 유지)와 본선 준비(기관 화면)뿐이다.

## 6. 브랜치·배포 관례 (8/16 도입)

- **`main` = 배포 브랜치.** Vercel Production이 여기 물린다. main은 항상 배포 가능 상태를 유지한다 — 머지 전 `npm test`·`npm run build` 통과가 조건
- **기능 작업은 브랜치에서.** 작업 단위 = 계획의 트랙 항목 하나. 명명: `feat/b2-mcp-tools` · `feat/s01-landing` · `feat/s06-validation` · `feat/b3-agents` · `chore/d3-rate-limit` 식
- **흐름**: 브랜치 생성 → 작업 커밋 → push → **작업 완료 후 이슈 생성** → **PR 생성**(본문에 `Closes #N`) → 셀프 리뷰(`/code-review` 활용 가능) → **squash merge**. squash라 main 히스토리가 "트랙 항목 = 커밋 1개"로 남는다.
  **⚠️ 머지 후 브랜치를 삭제하지 않는다**(2026.08.16 확정) — 작업 이력을 남겨두고, 필요 시 되짚어 볼 수 있게 한다. GitHub PR 화면의 "Delete branch" 버튼을 누르지 말 것
- **⚠️ 이슈를 미리 쌓아두지 않는다** — 착수 전 이슈 생성 금지. 계획 정본은 본 문서(HANDOFF)이고, 이슈는 **끝난 작업의 실행 기록**이다. 미리 만들면 계획을 중복 서술하는 동기화 대상이 하나 더 늘어난다 (2026.08.16 확정)
- **타입 접두사 5종** — `feat/` `fix/` `chore/` `docs/` `refactor/`·`test/`. 영역 접두사(`be/`·`fe/`)는 쓰지 않는다 — **명세 ID가 영역을 이미 담는다**(`s06`=화면, `b2`=백엔드, `d3`=배포). 단일 Next.js 앱이라 화면 작업도 `src/app`과 `src/lib`을 함께 건드린다
- Vercel이 브랜치별 **Preview URL을 자동 발급**한다 — 머지 전 확인에 쓴다
- **예외 — main 직커밋 허용**: HANDOFF·docs 문서 수정, 데이터 스크립트 소수정처럼 배포 산출물에 영향 없는 변경. 1인 개발이라 여기까지 PR을 강제하면 속도만 죽는다
- **⚠️ 9/7 11:00~9/11 23:59 무중단 구간 = main 머지 금지.** Vercel 자동 배포가 main에 물려 있으므로 머지가 곧 배포다. 심사 기간 작업(본선 준비)은 브랜치에만 쌓는다
- **9/7 동결 시점부터 `develop` 브랜치 도입** — main이 얼어 있는 동안 본선 작업(기관 화면 4종)의 통합 지점. `feat/* → develop`으로 머지해 모으고, 심사 종료(9/11 이후) 후 develop → main 일괄 머지. dev 브랜치는 상시 운용하지 않는다 — "main을 열 수 없을 때"만 존재 이유가 있다
- ⚠️ **Preview URL은 Vercel SSO 보호를 받는다** — 브라우저에 Vercel 로그인이 없으면 302로 막힌다. **카카오톡 인앱 브라우저 실기 확인(N-701·702)은 프로덕션 머지 후** 하거나, 필요 시 Settings → Deployment Protection에서 일시 해제할 것
- **GitHub Actions PR 체크 도입 완료(8/17)** — `.github/workflows/pr-check.yml`. typecheck · lint · test를 돌린다. **배포 게이트 3종(N-501 PII · N-405 환각 대조 · N-403 로그 정책)이 여기서 상시 검증된다**
  - ⚠️ **빌드는 CI에서 돌리지 않는다** — S-06이 빌드 시점에 `validation_stats`를 읽어 정적 생성되므로(DB 장애에도 화면이 살아 있게 하려는 의도, EX-402) 빌드에 DB가 필요하다. CI에는 자격증명이 없으니 **빌드는 Vercel이 실환경에서** 수행하고 PR에 상태를 단다
  - ⚠️ `npm run typecheck`는 **`next typegen`을 먼저 돌려야 한다** — `LayoutProps`·`PageProps`가 Next 생성 타입이라 `.next/types` 없이는 tsc가 못 찾는다. 스크립트에 넣어 뒀다(로컬은 빌드 흔적 때문에 통과하고 CI만 깨지던 원인)

## 7. 작업 방식 관례 (사용자와의 협업 스타일)

- 사용자는 **터미널·브라우저에 복붙해서 실행**하고 결과를 붙여넣는 방식으로 진행한다. 명령은 완성형으로, 한 단계씩, 기대 출력과 함께 제시할 것
- 에러가 나면 원문 그대로 받아서 원인 규명 → 수정 → 재실행 루프
- 파일은 폴더 연결 후 Claude가 직접 작성/수정하는 게 빠르다
- 검증 없이 넘어가지 않는다 — 각 단계마다 확인 쿼리/명령 포함
- ID 체계: SR(범위)·F(기능)·DR(데이터)·S(화면)·P/PR(권한)·E/EC(연동)·EX/EP(예외)·N(비기능)·Q(품질)·W(용어)·D(DB원칙)·PA(기획서조치)·U(미결, 전부 종결됨)
- **⚠️ 명세는 `docs/`를 먼저 읽고 먼저 고친다. 노션 정본은 나중에 일괄 반영한다 (2026.08.16 변경).** 노션 커넥터가 다른 워크스페이스에 물려 있어 매번 재연결하는 비용이 크다. 절차: ① 개발 근거는 `docs/` ② 수정은 `docs/`에 즉시 반영하되 grep 전수 검색으로 빠짐없이 ③ **노션 미반영분은 아래 「노션 반영 대기」에 누적** → 적절한 시점에 몰아서 반영 (N-801은 최종적으로 충족돼야 한다).

### 노션 반영 대기 (2026.09.01~)

| 정본 | 무엇 |
|---|---|
| 화면 요구사항 | **S-02 결과 단계를 절 단위 화면으로 재기술** — 입력과 같은 URL 질의(`view=`)로 3~5단계, 순서는 이용자의 시간 순서(물어볼 것 → 멈춤 신호 → 확인 전화 → 챙길 자료 → 근거). 단계 수는 조건부 절 때문에 조합마다 다르다 · **「전부 한 화면에서 보기(인쇄용)」**(`view=all`)가 인쇄와 데모의 경로 · 열거 밖·없는 절은 첫 절로 복귀 · 화면 제목 「이 조합에서 많았던 분쟁」 → **「이 조합에서 확인하실 것」**(전부 보기의 `h1`, 절 화면에서는 그 절 제목이 `h1`) |
| 기능 명세서(제출용) | 흐름도 박스 순서 · 사용자 시나리오 5·5-1 · **심사 동선 A-6 · B-4 · B-5 · B-5-1** |

**왜 이렇게 됐는지도 함께 옮긴다** — 입력 단계는 「한 화면에 하나의 질문」(A11Y-4)인데 결과만
두루마리였다. 실측(375×812, 펀드·신탁×창구): **6,558px = 8.1화면 · 항목 29개**. 중간에 앞머리
요약+앵커를 넣어 봤지만 첫 질문이 705px로 밀리고 「하실 일 19가지」로 읽혀 **걷어냈다.**
나눈 뒤 1단계 2,089px(2.6화면). 집계는 삭제·축약·접기 없이 전량 유지한다(F-202).

**제출용 문서라 심사 동선(A·B)이 실물과 어긋나면 바로 드러난다** — 반영 시 이 두 표를 먼저 맞춘다.
`docs/kakao-inapp-checklist.md` 【3-2】의 실기 점검 3줄은 사본 전용이라 노션 반영 대상이 아니다.

### 노션 반영 — ✅ 완료 (2026.08.21)

**대기 20건 전부 반영.** 정본 7종을 고쳤다 — 기획서 · 서비스 범위 · 기능 · 데이터 · 화면 · 외부 연동 · 비기능 · 용어 · DB 명세서.

**반영하다가 docs/ 자체의 누락 6곳을 찾았다.** 8/17 수치 교체 PR이 `08-nfr`과 `01-scope`만 고치고 나머지를 놓쳤다 — 즉 **노션뿐 아니라 docs/도 옛 수치를 들고 있었다**:

| 위치 | 무엇이 어긋나 있었나 |
|---|---|
| `08-nfr.md` N-602 | 커버리지 목표가 66\~71%로 남아 있었다. **목표로 남겨 두면 커버리지를 올리라는 압력이 되어 임계 4를 흔든다** |
| `04-screen.md` S-06 | 화면이 84%·66\~71%를 공개하도록 명세돼 있었다 — **코드는 이미 90%·33%를 공개 중** |
| `03-data.md` DR-106 | 단일 출처 정의가 옛 수치 |
| `02-functional.md` F-403 | 공개 대상 수치가 옛 값 |
| `01-scope.md` A-6 | 「커버리지 66\~71% 변동」 |
| `09-glossary.md` 병기 원칙 | 예시가 전부 실험 수치 |

**기획서는 수치를 고쳐 쓰지 않았다** — pilot 실험이 실제로 수행된 기록이라 지우면 측정 이력을 위조하게 된다. 대신 **「이 수치는 제품 성능이 아니다」 정정 주석**을 KPI 표 두 곳에 달고 제품 재측정값과 S-06 포인터를 함께 실었다.

**표 셀 줄바꿈은 `<br>`다** (`<br/>`도 Notion이 정규화한다). `~ [ ] $ < > { }`는 백슬래시로 이스케이프해야 한다 — 안 하면 취소선·링크로 먹힌다. 한 번에 보내는 payload는 나눠야 한다(길면 잘린다).

  - **측정 역사 서술은 고치지 않는다** — 기획서 10.1의 모집단(227/235건)처럼 «당시 측정이 실제로 수행된 기록»은 사실이다. 갱신 대상은 «현재 자산이 몇 건인가»를 말하는 서술뿐이다. 이 둘을 섞으면 측정 기록을 위조하게 된다

### ✅ 노션 반영 — 제출용 2종 완료 (2026.08.28)

커넥터가 **「2026 금융 AI Challenge」 워크스페이스**에 붙어 있는 것을 확인하고
제출용 문서 2종을 노션 페이지에 직접 옮겼다.

| 노션 페이지 | 출처 |
|---|---|
| 기획서(제출용) `3c60f70dd29c804e9b11e7fc879ee918` | `docs/기획서(제출용).md` |
| 기능 명세서(제출용) `3c60f70dd29c80d5ab6bf235bf7c97ba` | `docs/기능 명세서(제출용).md` |

**변환 시 주의** — 노션은 마크다운 파이프 표를 모른다. `<table>` 태그로 바꿔야
하고, 셀 안의 `n<20` 같은 부등호는 `\<`로 이스케이프해야 태그로 읽히지 않는다.
줄머리 `>`(인용)는 이스케이프하면 안 된다. 변환기는
`scratchpad/md2notion.py`에 있으나 세션 스크래치라 영구 보관본은 아니다.

**아래 목록은 정본(명세서 계열) 반영분이며 여전히 대기 중이다.**

### ✅ 노션 반영 — 정본 계열 완료 (2026.08.28)

대기 중이던 **16건을 노션 정본 6종에 반영했다.** 이제 `docs/` 사본과
노션 정본이 같은 내용을 말한다.

| 노션 페이지 | 반영 내용 |
|---|---|
| 기획서 | 1차 제출 범위(3축+MCP) · 제출 등급(기관 화면 구현 완료) · 데이터 이용 근거 「인용」 절 신설 · 로드맵 1단계 · 심사 후 계획 · 미결 A-4 |
| 서비스 범위 요구사항 | SR-103 포함 전환 · SR-209\~212 신설 · SR-X01·SR-X08 해제 · A-4 정정 · **9.1 변경 이력(R-01·R-02) 신설** · 축소 순서 0번·0-1번 |
| 화면 요구사항 | 총괄 표에 S-09\~S-13 · S-03 약관 붙여넣기 · S-04 「본문 길이」·「결과 저장·인쇄」 · S-08 표현 규칙 정정 · **S-09\~S-12 상세 절 + S-13 절** 신설 |
| 데이터 요구사항 | DR-101 원문 링크 칸 정정(DB가 본문을 담는다는 사실) · 9장 라이선스를 「인용」 근거로 전면 교체 |
| 비기능 요구사항 | N-701 실기 완료(1회차 완주·2회차 복구·iOS가 실제로 연결을 끊는다) · N-501 접근성에 기관·개발자 화면 6개 100점 |
| DB 명세서 | `facts_summary` 실측 길이와 「화면으로 나가는 것은 발췌뿐」 경계 |

**반영하며 발견한 것** — `docs/04-screen.md`에 **S-04 「본문 길이」 항목이 아예 없었다.**
HANDOFF는 대기 목록에 걸어 뒀는데 사본에 쓰이지 않은 채였다(구현은 되어 있었다).
사본을 먼저 채우고 노션에 넣었다.

**변환 주의** — 노션 마크다운은 `<br/>`이 아니라 `<br>`를 쓰고, 셀 안의 `n<20` 같은
부등호는 `\<`로 이스케이프해야 태그로 읽히지 않는다. 줄머리 `>`(인용)는 그대로 둔다.

### 노션 반영 — ✅ 완료 (2026.08.24, 실기 이전분)

정본 3종을 고쳤다.

| 정본 | 무엇 |
|---|---|
| 예외처리 요구사항 | **EX-302** 재시도 버튼 추가·410만 세션 파기(429·503·5xx는 보존) · **EX-403** 새로고침으로 사라지는 것은 판단 결과이지 상담 내용이 아니다 · **EX-405** 세션을 버리지 않는다 |
| 화면 요구사항 | **T-5** 봉인 토큰 구조에 맞춰 갱신(사라지는 것/남는 것/자동 재실행 금지) · **S-03 되묻기** 연월 입력의 iOS 실측 3건 |
| 비기능 요구사항 | **N-701** 8/21 부분 실측 + 8/24 사전 점검 + 8/24 시뮬레이터 전 구간 재주행(결함 2건) |

**실기 결과가 나오면 N-701을 한 번 더 고쳐야 한다** — 그때는 「배포 게이트는 아직 열리지 않았다」 문단을 결과로 대체한다.
