# 프리케이스(PreCase) — 개발 규칙

2026 금융 AI Challenge 출품작. 불완전판매 분쟁 예방·판단 AI 에이전트 웹 서비스.

**이 파일은 요구사항 명세서의 축소 불가 규칙만 뽑은 것이다.** 상세는 `docs/` 참조.
충돌 시 우선순위: `docs/01-scope.md` > `docs/`의 나머지 > 본 파일 > 코드.

---

## 🔒 절대 규칙 — 위반 시 설계 훼손

### 1. 환각 방지 (F-603 · C-6)

> **도구가 반환하지 않은 것은 화면에 렌더링되지 않는다.**

- 프롬프트 지시가 아니라 **렌더링 계층에서 구조적으로 강제**한다.
- 근거 표시 컴포넌트는 **도구 반환 객체만** props로 받는다. 문자열을 받지 않는다.
- 조문·판례·사례·통계를 모델이 생성하면 화면에 나올 수 없는 구조여야 한다.

```ts
// ✅ 이렇게
<StatuteCitation source={toolResult.statute} />
// ❌ 이렇게 하면 안 됨
<div>{llmOutput.statuteText}</div>
```

### 2. 계층 격리 (SR-402 · P-401)

③ 판단 계층 호출 페이로드에 **사용자 원문 필드가 존재하지 않아야 한다.**

| 계층 | 원문 | 슬롯 | 도구 호출 | 판단 결과 |
|---|---|---|---|---|
| ① 상담 | ✅ 읽기 | ✅ 쓰기 | ❌ | ❌ |
| ② 조사 | ❌ | ✅ 읽기 | ✅ | ❌ |
| ③ 판단 | **❌ 절대** | ✅ 읽기 | ❌ | ✅ 생성 |
| ④ 안내 | ❌ | ✅ 읽기 | ❌ | ✅ 읽기(수정 불가) |

타입으로 강제할 것 — `JudgeInput`에 `rawStatement` 같은 필드를 두지 않는다.

### 3. 저장 금지 (DR-4xx · N-403)

**서버·DB·로그 어디에도 저장하지 않는다:**
- 사용자 진술 원문 (마스킹 후에도)
- 확정 슬롯 값
- 판단 결과·확신도
- 실행 로그
- 민감정보 동의 기록

DB에 이 테이블이 있으면 그 자체가 결함이다. 서버 로그·APM에도 본문이 남으면 안 된다.
허용 로그 필드: 익명 세션 해시 · 이벤트 유형 · 도구명 · 소요 시간.

### 4. 확신도 임계 (F-305)

- 확신도 = 판단 에이전트의 **프롬프트 자기평가 1~5 정수** (로그확률 아님)
- **4점 미만 → 결론 미표시, 유보 경로**
- 임계값은 하드코딩 금지, 설정으로 관리 (모델 교체 시 재보정)
- 화면에 **백분율·승소 확률로 변환 표시 금지**

### 5. 법적 경계 (SR-X02~X06)

만들면 안 되는 것:
- 민원 신청서·법률문서 초안 생성 (변호사법 제109조)
- 로그인·회원가입·상담 이력 저장
- 유료화·광고·변호사 연계
- 금융회사 실명 + 평가 서술
- 파일 업로드 UI (1차 범위 밖 — 약관은 대화창 텍스트로 받는다)

출력 문구:
- ❌ "배상받을 수 있습니다"
- ✅ "유사 사례에서 배상이 인정되었습니다"

---

## 아키텍처

```
사용자 진술
  ↓
① 상담  되묻기 · 슬롯 확정 · 사실관계 정제
  ↓
② 조사  MCP 도구 5종 오케스트레이션
  ↓
━━━ 편향 차단 경계 ━━━
  ↓
③ 판단  정제 사실관계 + 근거만 수신 → 결론 + 확신도
  ↓
확신도 < 4 → 유보 경로
  ↓
④ 안내  용어 풀이 · 다음 행동 (판단 수정 불가)
```

**MCP 도구 5종** — 전부 읽기 전용, 쓰기 연산 없음

| 도구 | 외부 의존 |
|---|---|
| `lookup_statute` | 법제처 API + 삭제 조문 스냅샷 |
| `search_precedent` | 법제처 API (**사건번호 정확 대조 필수**) |
| `search_case` | 없음 (DB) |
| `check_documents` | 없음 (DB) |
| `analyze_risk_pattern` | 없음 (DB, 사전 집계값만) |

**`lookup_statute` 분기는 조문 종속** — 기준일 단독 분기가 아니다:
- 요청 조문이 금소법 이관 삭제 조문(6건)이고 기준일 < 2021.3.25 → 내장 스냅샷
- 그 외 전부(상법·민법·약관규제법·할부거래법, 보험업법 제95조의2 존치 항 포함) → 기준일 무관 현행 API

**`search_precedent` 알려진 결함** — 법제처 검색이 부분일치로 동작한다. `2010다76368` 조회 시 무관한 2026년 사건이 1순위로 반환됨을 실측 확인. **반환 사건번호가 요청과 정확히 일치하지 않으면 표시 불가 플래그**를 붙인다. 캐시 적중이어도 대조는 항상 수행.

**예방 축은 모델 미사용** — 사전 점검(F-201~203)은 구조화 폼 + 도구 반환값 템플릿 렌더링. Claude API를 호출하지 않는다. 모델 장애 시에도 정상 동작해야 한다.

---

## 슬롯

**필수 5종**

| 슬롯 | 코드 | 값 |
|---|---|---|
| 판매채널 | `channel` | TM / BANCA_HS / AGENT / BRANCH / ONLINE / UNKNOWN |
| 연령 | `age` | **가입 당시** 만 나이 19~120 (60↑ → ELDER 자동 파생) |
| 상품군 | `product` | 13코드 (아래) |
| 가입시점 | `contract_ym` | YYYY-MM (기준일 산정 근거) |
| 소비자 특성 | `traits` | PRO / ELDER / INEXP / CAPACITY / NONE (복수) |

**조건부 3종** — 발동 조건 충족 시 **필수로 승격**

| 슬롯 | 발동 조건 |
|---|---|
| `confirm_call` | 보험 + 설명의무 쟁점 |
| `survey_writer` | 적합성 원칙 쟁점 |
| `explained_loss` | 투자성 상품(`INV_*`) + 설명의무 쟁점 |

**상품군 13코드**
```
INS_SILSON INS_WHOLE INS_ANNUITY INS_SAVINGS INS_AUTO INS_ETC
INV_ELS INV_FUND INV_MARGIN INV_ETC
BNK_LOAN BNK_ETC
ETC_UNKNOWN
```

열거 변경 시 **기능 명세 · 데이터 명세 · DB 명세 · 마이그레이션을 동시 갱신**한다.

---

## 화면 8개

**필수 6** — 랜딩 · 사전 점검 · 상담 대화 · 판단 결과 · 근거 상세 · **검증 결과**
**조건부 2** — 자료 준비 체크리스트 · 실행 로그 (독립 화면이 어려우면 판단 결과 내 섹션으로 흡수, **출력 자체는 필수**)

⚠️ **검증 결과 화면(S-06 / SR-206)은 축소 불가다.** 정확성 관리 정책의 구현체라 위 절대 규칙 5종과 동급으로 취급한다 — 일정이 밀려도 이 화면은 빼지 않는다 (`docs/01-scope.md` 범위 변경 절차).

**검증 결과 화면(S-06) 렌더링 규약** — 절대 규칙 1(환각 방지)과 **같은 방식으로 구조적으로 강제**한다.

- 지표 컴포넌트는 `validation_stats` **행 객체 전체**를 받는다. 숫자만 뽑아 넘기지 않는다
- `note`는 **필수 필드**다. optional로 두지 않는다 — 수치만 렌더하고 단서를 빠뜨리는 코드가 타입 체크를 통과하면 안 된다
- **업권별 행은 퍼센트 단독 표시 금지** — n·신뢰구간·note를 항상 동반한다. (카드 n=5 · CI [23.1, 88.2]처럼 구간이 넓어 단독 수치가 오해를 만든다)
- 신뢰구간은 **로더가 실측 카운트에서 Wilson으로 계산**한다. 컴포넌트에도 문서에도 CI를 하드코딩하지 않는다
- 한계 항목(UNVERIFIED)을 접거나 숨기는 UI 금지 — 화면 명세 S-06 금지사항

기관 화면 4종은 본선 범위. 만들지 않는다.

**판단 결과 화면의 「가능성 낮음」·유보 상태에는 반드시 포함:**
① 신청권이 이용자에게 있음 명시 ② 금감원 1332 안내 ③ 신청을 말리는 표현 금지 ④ 판단이 달라지는 조건 제시

**접근성 (고령층이 주 사용자)**
- 본문 18px 이상 · 터치 영역 48×48px 이상 · 대비 4.5:1 이상
- **한 화면에 하나의 질문**
- 열거형 슬롯 질문은 자유 입력이 아니라 **선택지 버튼** ("모름" 상시 제공)
- 카카오톡 인앱 브라우저에서 동작해야 함

---

## 상한값

| 항목 | 값 |
|---|---|
| 도구 호출 | 사이클당 12 · 세션당 30 |
| 되묻기 | 사이클당 8 · 되돌림 포함 총 10 |
| IP 레이트 | 분당 30 · 세션 생성 시간당 5 · 판단 일 10 |
| 진술 입력 | 1회 4,000자 |
| 세션 유휴 | 30분 후 파기 |
| 캐시 TTL | 1일 (**만료 행 삭제 금지** — 만료분이 폴백 스냅샷) |

**성능 목표** — 정적 LCP 2.5초 · 상담 턴 P95 20초(스트리밍 시작 5초) · 판단 P95 120초 · 동시 10세션

⚠️ 판단 파이프라인이 120초라 **Vercel 함수 실행 시간 제한**에 걸린다. `maxDuration` 설정 + 스트리밍 구조 필수.

---

## 검증 수치 — 병기 원칙

화면·문서에 쓸 때 항상 이 형식으로:

**제품 기준 (2026.08.31 공식 런 · 검증셋 120건 × 재현 2회) — 화면이 공개하는 값**
- 결론 구간 정확도 **92.3% (60/65)** · CI [83.2, 96.7] — 회차별 93.1% · 91.7%
- 결론을 내는 비율(커버리지) **27.1% (65/240)** · CI [21.9, 33.0] — 회차별 24.2% · 30.0%
- **두 번 모두 결론을 낸 사건 25/120, 그 정확도 96% (24/25)** — 재현을 요구하면
  커버리지가 낮아지는 대신 정확도가 오른다
- **유보 구간 정확도 64.6% (113/175)** — 이용자에게 불리해도 함께 공개한다
- 확신도 분포 rep1 2:5·3:86·4:28·5:1 / rep2 2:7·3:77·4:34·5:2

⚠️ **회별 값을 반드시 병기한다.** 커버리지는 재 볼 때마다 24~33%로 흔들리는 값이고,
한 번 잰 값만 걸면 그 흔들림이 수치 뒤에 숨는다. 이전 단일 런 값(8/17 · 90%·33.3%)은
이 공식 런으로 대체됐다 — 실험 수치가 아니라 같은 과제의 재측정이다.

**안전성 시험 (2026.08.31 · A5 레드팀)** — 인젝션 7/7 무해화(오탐 0) · PII 변형 9/9 ·
유도 질문 6/6 방어. **이 시험이 실제 구멍 하나를 찾았다**(주민번호 앞자리가 하이픈 없이
오면 미검출 — 같은 날 수정). 공격 문안을 직접 썼다는 한계를 함께 공개한다

**이전 실험 (보존 — 다른 과제를 잰 값)**
- 결론 구간 84% (71/85) · 커버리지 66~71% · 전체 76%
- ⚠️ **이 값을 제품 성능으로 쓰지 마라.** 완결된 조정례 결정서를 넣고 위원회 결정을
  예측한 실험이고, 제품은 소비자 진술에서 정제한 사실관계로 판단한다. 다른 과제다.
  실험 수치를 화면에 걸어 두었던 것이 2026.08.17에 바로잡은 문제다

업권별 편차는 **n·재현 변동 폭 병기** (하위 그룹을 성능 근거로 쓰지 않는다)

수치의 단일 출처는 DB `validation_stats`이고, 그 원자료는 `measure/`에 있다.
여기 값과 화면·문서가 어긋나면 안 된다.

**채점 매핑** — 판단 출력 ↔ 조정례 라벨: 높음 ↔ `UPHELD` / 낮음 ↔ `REJECTED`
(PARTIAL은 실측 0건이라 스키마에서 제거 — 마이그레이션 0003. 일부 인용은 `order_type`에 보존)

---

## 기록 규칙 — 커밋·이슈·PR

**커밋 메시지에 AI 서명·공동 저자 트레일러를 넣지 않는다.**

```
❌ Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
❌ 🤖 Generated with [Claude Code](…)
```

도구가 기본값으로 붙이므로 **매번 의식적으로 빼야 한다.** 커밋 히스토리는
심사에서 읽히는 산출물이고, 트레일러가 붙은 커밋은 되돌리기 어렵다.

커밋·이슈·PR 본문은 **사람이 쓴 것처럼** 쓴다. 길이는 변경 크기에 비례하고,
한 줄 변경에 다섯 문단을 달지 않는다. 문단 끝마다 교훈을 붙이지 않는다.
이모지를 쓰지 않는다. 상세 규칙은 `.github/ISSUE_PR_PLAYBOOK.md`.

---

## 배포 전 게이트

- [ ] PII 마스킹 합성 100건 테스트 — **미검출 0건** (배포 조건)
- [ ] 도구 반환값 ↔ 화면 인용 자동 대조 테스트 CI 포함
- [ ] 서버 로그에 진술·슬롯·판단 본문이 남지 않는지 전수 점검
- [ ] Lighthouse 접근성 90점 이상
- [ ] 데모 3종이 **DB가 아니라 배포 번들**에 있는지 (DB 장애에도 열람 가능해야 함)
- [ ] 카카오톡 인앱 브라우저 실기 확인

## 일정

마감 **2026.09.07 10:00** / 목표 제출 09.06 / 범위 동결 09.03 / 무중단 09.07 11:00~09.11 23:59
