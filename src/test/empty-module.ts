/**
 * `server-only` 대체 스텁 (테스트 전용).
 *
 * `server-only`는 react-server 조건이 없으면 "클라이언트에서 쓸 수 없다"는 오류를
 * 던지도록 설계돼 있다. vitest는 그 조건을 켜지 않으므로 서버 모듈을 직접 부르는
 * 테스트가 전부 깨진다. 프로덕션 빌드에서는 실제 패키지가 그대로 동작하므로
 * (next build가 잘못된 import를 여전히 잡는다) 이 스텁은 테스트 경로에만 쓴다.
 */
export {};
