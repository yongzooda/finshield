<!-- 원본: https://app.notion.com/p/3ba0f70dd29c80b19f02dac008137908 · export 2026-08-15 · 정본은 노션, 이 파일은 참조 사본 -->
# 비기능 요구사항
프리케이스(PreCase) — 2026 금융 AI Challenge 1차 심사 제출본
<callout icon="🎯" color="blue_bg">
	**이 문서의 역할** — 기능이 아닌 품질 속성(성능·상한·가용성·보안·개인정보·품질 KPI)을 수치로 확정한다. 앞선 문서들이 이관한 **미결 5건(U-4 · U-6 · U-8 · U-10 · U-11)을 전부 여기서 종결**한다.
	수치 중 「초기값」 표기는 운영 측정 후 조정 가능하되, **조정 시 본 문서를 갱신하고 근거를 남긴다.** 측정 없이 수치를 바꾸지 않는다 — 기획서 10.4의 원칙(측정하지 않은 것은 주장하지 않는다)을 운영에도 적용한다.
</callout>
## 1. 문서 정보
<table header-row="true">
<tr>
<td>항목</td>
<td>내용</td>
</tr>
<tr>
<td>문서명</td>
<td>비기능 요구사항</td>
</tr>
<tr>
<td>상위 문서</td>
<td>범위(C 제약) · 기능(F) · 권한(P) · 외부 연동(EC) · 예외처리(EX) 요구사항</td>
</tr>
<tr>
<td>작성 기준일</td>
<td>2026.08.14</td>
</tr>
<tr>
<td>N-ID 체계</td>
<td>N-1xx 성능 · N-2xx 상한·용량 · N-3xx 가용성·운영 · N-4xx 보안 · N-5xx 개인정보 · N-6xx 품질 KPI · N-7xx 호환성 · N-8xx 유지보수</td>
</tr>
</table>
## 2. 미결 종결 총괄
<table header-row="true">
<tr>
<td>미결</td>
<td>확정 내용</td>
<td>상세</td>
</tr>
<tr>
<td>**U-4** 도구 호출 상한</td>
<td>판단 사이클당 **12회** · 세션당 **30회** (초기값)</td>
<td>N-201</td>
</tr>
<tr>
<td>**U-6** 성능 목표</td>
<td>상담 턴 P95 **20초**(스트리밍 시작 5초) · 판단 파이프라인 P95 **120초** · 정적 화면 LCP **2.5초**</td>
<td>N-101\~103</td>
</tr>
<tr>
<td>**U-8** 되묻기 상한</td>
<td>판단 사이클당 **8회**, 되돌림 포함 총 **10회** (초기값)</td>
<td>N-202</td>
</tr>
<tr>
<td>**U-10** 동의 기록 보존</td>
<td>**미보존 확정** — 저장된 민감정보가 존재하지 않으므로</td>
<td>N-503</td>
</tr>
<tr>
<td>**U-11** 남용 방어</td>
<td>IP당 분당 요청 **30** · 상담 세션 생성 시간당 **5** · 판단 일 **10회** (초기값)</td>
<td>N-203</td>
</tr>
</table>
---
## 3. N-1xx 성능
측정 기준: 모바일 4G · P95. **정직한 목표를 걸고 로딩 경험(화면 4.3)으로 보완**한다 — 4계층 Opus 5 파이프라인에서 초단위 응답을 약속하는 것은 허위다.
<table header-row="true">
<tr>
<td>ID</td>
<td>항목</td>
<td>목표</td>
<td>비고</td>
</tr>
<tr>
<td>**N-101**</td>
<td>정적 화면 (S-01·02 입력·06, 데모)</td>
<td>LCP **2.5초 이하** · TTFB 1초 이하</td>
<td>Vercel 엣지 + 정적 생성</td>
</tr>
<tr>
<td>**N-102**</td>
<td>상담 턴 (되묻기 1회 응답)</td>
<td>**스트리밍 시작 5초 이내 · 완료 P95 20초**</td>
<td>스트리밍 필수 — 첫 토큰이 빨라야 대화감 유지</td>
</tr>
<tr>
<td>**N-103**</td>
<td>판단 파이프라인 전체 (조사+판단+변환)</td>
<td>**P95 120초 이내**</td>
<td>도구 호출 수에 비례. 로딩 단계 문구 필수 (4.3) — "무단계 스피너 금지"가 이 목표의 짝<br/>**실측 (2026.08.16, TM·67세·INS_WHOLE·2019-05·설명의무)** — 전체 **88.6초 / 93.2초**(2회). 내역: ② 조사 49~51초(도구 9~10회) · ③ 판단 39~42초 · ④ 안내 0.2초(유보는 모델 미호출). **결론 경로가 최악**이라 `guide()` 모델 호출을 따로 측정 → **7.1초**. 합산 최악 **약 100초**로 목표 안. 첫 이벤트는 **1ms**(N-102 여유)<br/>**⚠️ Vercel 함수 실행 시간 제한에는 걸리지 않는다** — Hobby는 Fluid compute 기본값으로 `maxDuration` **기본 300초·최대 300초**다(공식 문서). 실측 최악 100초는 상한의 약 33%. **그래도 스트리밍은 필수인데, 이유가 함수 제한이 아니다** — Vercel 문서: HTTP/1.1에는 HTTP/2의 PING에 해당하는 프레임이 없어 **중간 네트워크 계층이 유휴 연결을 닫을 수 있으므로 작업 중 진행 데이터를 흘려보내야 한다.** 카카오톡 인앱(N-701) 경로에서 특히 중요<br/>**프로덕션 스트리밍 실측** — `precase.vercel.app`에서 **120초·240초** 스트리밍이 5초 간격 하트비트로 **버퍼링 없이 실시간 도달**(벽시계와 경과시간 일치, 240초도 끊김 없이 완주). 실측 최악 100초의 2.4배까지 여유가 확인됐다. 응답 헤더에 `no-transform`·`X-Accel-Buffering: no` 필요</td>
</tr>
<tr>
<td>**N-104**</td>
<td>사전 점검 결과 (F-202·203)</td>
<td>P95 **10초 이내**</td>
<td>사전 집계 조회 중심이라 판단보다 훨씬 빨라야 한다</td>
</tr>
</table>
## 4. N-2xx 상한·용량 — 4중 방어의 수치
비용 폭주 방어는 **세션 내(N-201·202) → IP 단위(N-203) → 전역(N-204)** 순으로 겹겹이 건다.
<table header-row="true">
<tr>
<td>ID</td>
<td>항목</td>
<td>값 (초기값)</td>
<td>초과 시</td>
</tr>
<tr>
<td>**N-201**</td>
<td>도구 호출 상한 (F-607 · U-4)</td>
<td>**판단 사이클당 12회 · 세션당 30회**</td>
<td>EX-204 — 수집분으로 판단 진행. 근거: 실행 로그 예시 기준 정상 사이클 약 6회 + 재시도·되돌림 여유 2배</td>
</tr>
<tr>
<td>**N-202**</td>
<td>되묻기 상한 (F-302 · U-8)</td>
<td>**사이클당 8회 · 되돌림 포함 총 10회**</td>
<td>EX-103 — 수집 슬롯으로 진행. 근거: 필수 슬롯 5 + 조건부 3 = 8</td>
</tr>
<tr>
<td>**N-203**</td>
<td>IP 레이트 리밋 (U-11)</td>
<td>**분당 요청 30 · 상담 세션 생성 시간당 5 · 판단 일 10회**</td>
<td>EX-405 — 대기 안내 + 차단 시간 명시. 정상 사용자 시나리오(재상담 포함)를 막지 않는 수준. **IP 카운터는 엣지 미들웨어 휘발 메모리에서만 처리(최대 24시간 후 자동 소멸), DB·서버 로그 저장 금지** — DR-30x 비식별 원칙과의 경계<br/>**✅ 구현 (2026.08.17)** — 실호출 확인: 31번째 요청에서 429, 「약 59초 후에 다시 시도하실 수 있습니다」<br/>**원본 IP를 메모리에도 두지 않는다** — 프로세스마다 다른 소금으로 해시해 앞 24자만 센다. 재시작하면 같은 IP도 다른 키가 된다. 테스트가 키에서 IP를 알아볼 수 없음을 고정<br/>**세는 단위를 나눴다** — 분당(모든 요청) · 세션 생성(시간당, **되묻기는 세지 않는다** — 재상담 F-308을 막지 않아야 한다) · 판단(일). 종류마다 따로 세므로 되묻기가 판단 상한을 깎지 않는다<br/>**⚠️ 「엣지 미들웨어」가 아니라 라우트 핸들러에 뒀다** — ① Edge 런타임은 `node:crypto`·`server-only`를 쓸 수 없다 ② Edge는 Node 함수보다 인스턴스가 많아 메모리 카운터가 **더** 흩어진다. 카운터를 일하는 함수와 같은 프로세스에 두는 편이 그나마 정확하다. 「휘발 메모리에서만·DB 저장 금지」라는 실질 요구는 그대로 지킨다<br/>**⚠️ 이 방어는 완전하지 않다 — 그것이 설계다** — Vercel은 인스턴스가 여러 개라 한 IP가 흩어지면 상한을 넘길 수 있다(세션 실측으로 확인한 성질). 명세가 방어를 겹겹이 둔 이유가 이것이다: 세션 내(N-201·202) → **IP(N-203)** → 전역 하드 쿼터(N-204). IP 층은 흔한 연타를 막는 과속방지턱이고 마지막 방어선은 전역 쿼터다. 완전한 IP 차단은 공유 저장소를 요구하는데 그건 금지돼 있다</td>
</tr>
<tr>
<td>**N-204**</td>
<td>Claude API 전역 하드 쿼터</td>
<td>**월 예산 C-4(8\~10만 원) 내 설정.** 일 단위 소프트 알림 80% · 하드 컷 100%<br/>**✅ 구현 (2026.08.17)** — 설정은 `DAILY_BUDGET_USD`(**달러**). 모델 단가가 USD 고시라 원화로 두면 환율이 두 번째 변수가 되고, 그 환율이 낡으면 쿼터가 조용히 어긋난다. 월 8~10만 원 ≈ $55~70 ≈ **일 $1.8~2.3**<br/>**계량은 DB에 날짜별 합계만** (`api_budget`, 마이그레이션 0006). 하드 쿼터는 전역이어야 하는데 Vercel은 라우트마다 별도 함수라 프로세스 메모리로는 각자 다른 숫자를 본다. 개별 호출 행을 남기지 않아 시각 상관관계로 세션을 재구성할 수 없다 — `usage_counters`(DR-302)와 같은 성격이다. `app_runtime`에 DELETE 권한 없음<br/>**거절·토큰 상한도 센다** — 이미 쓰인 토큰이다. 성공만 세면 실패가 잦을수록 쿼터가 실제보다 낮게 보인다<br/>**실패 방향을 나눈다 (EP-3)** — 기록 실패는 **삼키고**(계량 때문에 판단이 막히면 안 된다), 조회 실패는 **막는다**(얼마 썼는지 모르는 채 쓰는 쪽이 위험하다)<br/>**모르는 모델이면 던진다** — 단가를 모르는 채 0으로 세면 쿼터가 있으나 마나가 된다<br/>**⚠️ 이 기능이 없어서 실제로 사고가 났다** — 검증셋 120건 측정 도중 크레딧이 5건 만에 소진됐고, 나머지 115건의 400 응답이 「형식 오류」로 집계돼 **커버리지 0.8%라는 가짜 수치**가 만들어졌다. 소진은 작업 도중이 아니라 **시작 전에** 드러나야 한다<br/>**⚠️ 반대 방향 사고도 났다 (2026.08.30)** — 측정 러너가 게이트는 거치지 않으면서 **계량만 공유 DB에 남겨**, 측정 런 다음날까지 프로덕션이 신규 상담을 503으로 거부했다(실측 spentUsd 9.14/2.0). 그래서 경계를 명시한다: **이 쿼터는 서비스(이용자 트래픽)의 지출 장치이고, 측정 러너(PRECASE_LIVE=1)의 사용량은 계상하지 않는다** — 측정 지출은 크레딧 잔액으로 따로 관리한다(SPRINT 상시 규칙). 오염된 당일 집계 행은 측정분을 걷어내 바로잡는다</td>
<td>EX-404 — 신규 상담 차단 + 운영자 즉시 알림. 심사 기간(C-2)에는 잔여 쿼터를 매일 점검</td>
</tr>
<tr>
<td>**N-205**</td>
<td>진술 입력 길이</td>
<td>1회 **4,000자** 이내</td>
<td>초과 시 나눠 입력 안내 — 비용·컨텍스트 방어</td>
</tr>
<tr>
<td>**N-206**</td>
<td>동시 상담 세션</td>
<td>목표 **10 세션** 동시 처리<br/>**⚠️ 세션 구조 변경 (2026.08.16) — 서버 메모리 → 봉인 토큰**<br/>당초 세션을 서버 프로세스 메모리 Map에 뒀으나 **Vercel에서 동작하지 않는다.** 라우트마다 별도 서버리스 함수로 배포되어 **메모리를 공유하지 않기 때문이다.** 실측: `/api/consult`는 같은 세션을 계속 알아보는데(200 → 410 → 200) `/api/judge`만 410. 로컬 `next dev`는 단일 프로세스라 결함이 드러나지 않아, **같은 함수 안에서만 측정한 「세션이 유지된다」는 결론이 틀렸다**(연속 5요청 44.8초 · 60초 공백 210초 — 둘 다 `/api/consult` 단독이었다).<br/>**해결** — 세션 상태를 **AES-256-GCM으로 봉인해 클라이언트가 들고 다닌다**. DB·KV에 두는 방법은 절대 규칙 3(DR-4xx 저장 금지)에 걸려 쓸 수 없다. 서버에는 아무것도 남지 않고, 봉인이라 브라우저는 열어볼 수 없다. 키는 기존 서버 비밀에서 HKDF로 파생한다(환경변수를 새로 요구하면 값을 넣기 전까지 배포가 죽는다). 유휴 만료 판정도 **봉인 안의 시각**으로 하므로 클라이언트가 되돌릴 수 없고, 도구 호출 카운터(F-607)도 봉인 안에 있어 초기화되지 않는다</td>
<td>심사·시연 규모 기준. 초과 시 대기 안내 (오류 아님)<br/>**✅ 실측 (2026.08.23) — 동시 10세션 통과.** 프로덕션 `/precheck`(모델 미호출·DB만으로 도는 경로) 동시 10회 실호출: **P95 3.1초** · 전체 벽시계 3.24초. N-104 목표 10초의 31%로 여유 3배<br/>**병목은 DB가 아니다** — 판단 1사이클의 DB 몫만 따로 재면 동시 10에서 세션당 p95 **0.21초**(단독 대비 2.8배, 동시 20에서도 0.44초). 3.1초의 대부분은 Vercel 함수 기동·렌더다. 따라서 **Supabase 플랜을 올려도 이 수치는 거의 그대로다**(D-5 재판정 근거).<br/>**⚠️ 이 측정이 결함을 잡았다 — 접속 모드** — `DATABASE_URL`이 **세션 모드 풀러(5432)**를 쓰고 있었다. 세션 모드는 클라이언트마다 연결을 붙들어 **`pool_size 15`에서 막힌다**(`EMAXCONNSESSION`). Vercel은 함수 인스턴스마다 풀(`max: 5`)을 만들므로 **인스턴스 3~4개만 떠도 한도**다. Supabase 문서는 서버리스·엣지에는 트랜잭션 모드를 쓰라고 권고하므로 **문서화된 안티패턴**이었다. **트랜잭션 모드(6543) + `prepare: false`**로 교체하니 동시 **50까지 오류 없이** 완주했다(세션 모드 한계의 3배 이상). **프로덕션 전환 후 실측(2026.08.23)** — 동시 25 실패 0·P95 3.04초 / 동시 **40** 실패 0·P95 **3.55초**(목표 10초의 36%). 목표 동시 10세션의 4배를 태워도 여유가 남는다. 다만 전환 전 프로덕션을 동시 40으로 재본 적은 없으므로 「전환으로 프로덕션이 살아났다」가 아니라 **「연결 상한이 사라졌다」**가 정확한 서술이다 — 응답 시간은 전후가 거의 같고(3.14초 → 3.04초) 병목은 여전히 Vercel 함수 기동이다. **플랜이 아니라 모드의 문제라 Pro로 바꿔도 고쳐지지 않았을 결함**이고, 심사 중 동시 접속이 몰렸을 때 드러났을 것이다. 원자료 `measure/concurrency.md`</td>
</tr>
</table>
## 5. N-3xx 가용성·운영
<table header-row="true">
<tr>
<td>ID</td>
<td>항목</td>
<td>요구사항</td>
</tr>
<tr>
<td>**N-301**</td>
<td>무중단 구간</td>
<td>**09.07 11:00 \~ 09.11 23:59** (C-2) — 이 구간 배포 동결. 배포는 09.06까지만</td>
</tr>
<tr>
<td>**N-302**</td>
<td>헬스체크</td>
<td>**5분 주기** — 핵심 경로 3종: 정적 화면 응답 · 법제처 API 도달성 · Claude API 도달성. 데모 열람 경로 포함<br/>**구현 분담 (B-4, 2026.08.16)** — `GET /api/health`가 **의존성 도달성**을 본다(법제처·Claude + **DB 추가**). DB는 명세 3종에 없지만 사전 점검(S-02)·검증 결과(S-06)가 DB만으로 동작하므로, DB 장애를 초록으로 보고하면 거짓이 된다(EX-402) — 축소가 아니라 추가다. **정적 화면·데모 열람은 감시자가 `/`와 데모 URL을 직접 조회**해 확인한다(순환 확인 회피). 응답 상태: ok·degraded → 200 / down → 503. **degraded는 상태코드로 드러나지 않으므로 감시자는 본문 `status`를 반드시 파싱**해야 한다 (D-4 구성 조건)<br/>**감시자용 엄격 모드 `?strict=1` (2026.08.23 추가)** — 위 규칙에는 현실적인 문제가 있다. **`degraded`가 200이라 상태코드만 보는 감시 도구는 놓친다.** 본문을 파싱하는 키워드 감시는 감시 서비스에서 흔히 유료라, 「본문을 읽어라」는 요구가 사실상 「유료 도구를 써라」가 된다. 그래서 옵트인 파라미터를 뒀다 — **`/api/health?strict=1`은 `ok`가 아닌 모든 상태를 503으로 반환**한다. 무료 HTTP 감시자도 `degraded`를 잡을 수 있고, 기본 경로(사람·대시보드가 읽는 쪽)의 의미는 그대로다. **감시자는 `?strict=1`을, 사람은 기본 경로를 본다.** 본문은 두 경로가 동일하다<br/>**✅ 감시자 구성 완료 (2026.08.23)** — UptimeRobot 무료 플랜, 모니터 3종 · 전부 5분 주기 · 실측 전부 `Up`.<br/>① `/api/health?strict=1` (응답 1,480ms — 외부 의존 3종을 실제로 찔러보는 값) ② `/` ③ `/demo`. ②③을 따로 두는 이유는 헬스체크가 **화면을 보지 않기** 때문이다 — 빌드가 깨져도 `/api/health`는 200을 준다. 특히 `/demo`는 다른 게 다 죽어도 살아 있어야 하는 마지막 경로다(F-701).<br/>**유료 기능에 기대지 않는다** — 키워드 감시·1분 주기·HTTP 메서드 지정·상태코드 목록 편집은 전부 유료였다. `?strict=1`이 그 자리를 대신한다(무료 HTTP 감시자가 상태코드만으로 `degraded`를 잡는다).<br/>알림은 이메일로 실제 도달 확인. 모니터 상세의 `Test Notification`으로 시험한다.<br/>**부수 효과** — 5분마다 DB를 치므로 Supabase Free의 「1주일 무활동 정지」 조건에 도달하지 않는다(D-5 재판정 근거). 11분 창에서 트랜잭션 266건 증가로 폴링을 교차 확인했다</td>
</tr>
<tr>
<td>**N-303**</td>
<td>장애 알림</td>
<td>헬스체크 실패 · 쿼터 80% 도달 시 **운영자 모바일로 즉시 알림** (1인 운영이므로 이메일만으로는 부족)<br/>**✅ 구현 (2026.08.17)** — 전달 채널을 코드에 박지 않는다. `OPS_ALERT_WEBHOOK_URL`로 POST 한 번을 쏘고, 그 주소가 Slack·Discord·ntfy 무엇이든 상관하지 않는다 — 채널을 박으면 그 서비스가 죽을 때 알림도 함께 죽는다. **웹훅 미설정 시** 알림 대신 `/api/health`의 `budget` 필드로 상태가 드러나므로 감시자가 읽어 알릴 수 있다. 둘 중 하나는 반드시 갖출 것<br/>알림 본문에 **진술·슬롯·판단이 들어가지 않는다**(N-403) — 나가는 것은 사건 종류와 숫자뿐. 같은 알림은 30분 억제</td>
</tr>
<tr>
<td>**N-304**</td>
<td>복구 목표 (심사 기간)</td>
<td>**대응 착수 1시간 · 복구 4시간** 이내 (1인 운영 현실 기준). 복구 불가 시 데모 경로 정상 여부를 최우선 확보 (EX 8장 매트릭스)</td>
</tr>
<tr>
<td>**N-305**</td>
<td>백업</td>
<td>정적 자산(DR-1xx) 버전별 보관 (데이터 8장) — DB 장애 시 재적재 가능해야 한다. 재적재 절차를 배포 전 1회 리허설</td>
</tr>
</table>
## 6. N-4xx 보안
앞선 문서의 보안 요구(F-6xx · P-4xx·5xx · EC-2·4)를 상속하고, 남은 운영 수준만 확정한다.
<table header-row="true">
<tr>
<td>ID</td>
<td>항목</td>
<td>요구사항</td>
</tr>
<tr>
<td>**N-401**</td>
<td>세션 식별자</td>
<td>**128비트 이상 난수** (UUID v4 이상) · httpOnly · **유휴 30분 시 서버 측 세션 메모리 자동 파기** (P-301의 수치화)</td>
</tr>
<tr>
<td>**N-402**</td>
<td>전송 보안</td>
<td>전 구간 HTTPS (TLS 1.2+). HTTP 접근은 리다이렉트</td>
</tr>
<tr>
<td>**N-403**</td>
<td>서버 로그·APM 정책</td>
<td>**진술 본문·슬롯 값·판단 내용을 서버 로그에 기록 금지** (DR 8장). 로그에는 익명 세션 해시 · 이벤트 유형 · 도구명 · 소요 시간만. 배포 전 로그 출력 전수 점검 1회<br/>**✅ 전수 점검 완료 (2026.08.17)** — 프로덕션 코드의 `console` 호출은 2건뿐이고 둘 다 이벤트 유형 + SQLSTATE만 남긴다(허용 필드 범위 내).<br/>**⚠️ 위반 1건 발견·수정** — `investigate.ts`가 도구 실패 시 **오류 메시지 원문**을 실행 로그 `detail`에 싣고 모델에도 되돌려 주고 있었다. postgres는 파라미터 값을 메시지에 담으므로(`invalid input syntax ... "값"`) **슬롯 값이 실행 로그와 모델 입력으로 새는 경로**였다. 분류만 남기도록 고쳤다(`LAW_<kind>` · `DB_<SQLSTATE>` · 예외 클래스명).<br/>**점검을 테스트로 고정했다** — 사람이 눈으로 훑는 점검은 다음 커밋에서 무너진다. 소스를 읽어 ① 승인되지 않은 `console` 호출 ② 오류 객체 통째 출력 ③ 실행 로그에 실린 오류 원문을 잡는다. 승인 목록은 **줄 내용까지 고정**하고 각 항목에 사유를 적게 했다. 가드가 실제로 결함을 잡는지 되돌려 넣어 확인했다</td>
</tr>
<tr>
<td>**N-404**</td>
<td>의존성 점검</td>
<td>배포 전 `npm audit` 수준의 알려진 취약점 점검. 심사 기간 중 신규 의존성 추가 금지 (N-301 동결과 연동)</td>
</tr>
<tr>
<td>**N-405**</td>
<td>환각 방지 자동 검증</td>
<td>**도구 반환값 ↔ 화면 인용 자동 대조 테스트**를 CI에 포함 — F-603이 회귀로 깨지는 것을 구조적으로 감지 (기획서 리스크 R-05 대응)<br/>**✅ 구현 (2026.08.17)** — 판단 결과 화면을 실제로 렌더해 화면의 조문번호·사건번호·의결번호가 전부 `evidence`에 실재하는지 대조한다. 픽스처에 **가짜 인용을 일부러 심어** 두고, 그것이 화면에 나오면 실패한다. GitHub Actions PR 체크에 포함(자격증명 불요).<br/>**⚠️ 이 테스트가 실제 구멍을 잡아냈다** — `guide.explanation`은 **모델이 쓴 산문**인데 화면이 그대로 렌더하고 있었다. 모델이 「민법 제750조에 따라…」를 쓰면 도구가 반환한 적 없는 조문이 화면에 나가는 경로였다 — CLAUDE.md가 금지한 `<div>{llmOutput.statuteText}</div>` 바로 그 형태다.<br/>**두 곳에서 막는다** — ① ④ 안내 계층이 모델 산문에서 근거 없는 인용이 든 문장을 제거 ② **화면이 다시 대조**. ①만 두면 「모델 출력을 거른다」는 약속이고, ②가 있어야 「화면에 나올 수 없다」는 **구조**가 된다(절대 규칙 1은 렌더링 계층 강제를 요구한다). `changesIf`도 모델 텍스트라 같은 대조를 거친다</td>
</tr>
<tr>
<td>**N-406**</td>
<td>안전성 시험 (레드팀)</td>
<td>**신설 (2026.08.31 · R-08 · S-06 v2 공개 대상).** F-601(PII)·F-604(범위 밖 거절)·F-606(도구 반환값 인젝션)은 각각 구현·단위 테스트가 있었지만, **공격자 관점에서 셋을 한꺼번에 두들겨 본 적이 없었다.** 방어를 따로 재면 「규칙에 걸리는 입력」만 통과하게 되고, 실제 미검출은 규칙이 상정하지 않은 표기에서 나온다(N-501의 성명 28건이 그랬다)<br/>**요구** — 세 방어에 공격 세트를 넣어 방어율을 재고, **결과를 S-06에 공개한다.** 인젝션·PII는 **모델 없이 결정적**으로 짜 CI에 상시 두고(자격증명 불요), 유도 질문만 상담 계층 실호출로 잰다<br/>**✅ 측정 완료 (2026.08.31)** — 인젝션 **7/7 무해화** · 정상 근거 3건 **오탐 0**(표기 위조 우회 차단 포함) · PII 변형 **9/9 가림 또는 차단** · 유도 질문 **6/6 방어**(범위 밖 거절 4 · 미끼 진술도 거절 2)<br/>**⚠️ 이 시험이 실제 결함을 하나 찾았다 — 그것이 이 항목의 존재 이유다.** **주민등록번호 앞자리가 하이픈 없이 오면 가리지도 막지도 않았다.** 「앞자리가 800101인 주민번호예요」는 ① 뒷자리가 없어 RRN 마스킹 규칙 밖이고 ② 잔존 의심 규칙이 `\d{6}\s*[-–—]`로 **하이픈을 요구**해 규칙 밖이었다. 생년월일이 그대로 모델로 갔다. **N-501의 합성 100건 세트에 이 변형이 없었고, 「미검출 0건」은 그 세트 안에서만 참이었다**<br/>· **수정** — 6자리 숫자 자체를 의심하면 안 된다. 이 도메인에서 10만\~99만 원 금액이 흔하다(「보험료 350000원」). **맥락 낱말이 가까이 있을 때만** 잡는다 — 주민(등록)번호·생년월일·앞자리가 숫자 앞뒤 12자 안에 있으면 차단. 오탐 점검: 금액 6자리 4종 전부 통과, 맥락 붙은 6자리 3종 전부 차단<br/>· **N-501과의 관계** — 배포 조건(미검출 0건)의 분모는 여전히 합성 세트다. **이 시험은 그 세트가 무엇을 놓치는지를 재는 별개 축**이고, 찾은 변형은 pii-n501 세트에 편입한다 — **✅ 편입 완료 (2026.08.31, 117 → 132건).** 레드팀이 찾은 것을 세트에 넣지 않으면 다음 회귀에서 같은 구멍이 다시 열린다<br/>**⚠️ 한계를 함께 공개한다 (S-06 필수)** — **공격 문안을 직접 썼다.** 규칙 기반 방어(인젝션·PII)라 규칙을 아는 사람이 만든 문안은 난이도가 실제보다 쉽거나 어려울 수 있다. 「막았다」가 아니라 **「이 세트 범위에서 관측된 방어율」**로 읽는다 — N-606의 「99.2%는 하한이 아니라 상한」과 같은 성질이다. 세트를 넓히면 더 나올 수 있고, 나오면 같은 방식으로 고치고 공개한다<br/>원자료 `measure/redteam.md` · `measure/redteam.jsonl` · 공격 문안 `redteam-fixtures.ts`</td>
</tr>
</table>
## 7. N-5xx 개인정보
<table header-row="true">
<tr>
<td>ID</td>
<td>항목</td>
<td>요구사항</td>
</tr>
<tr>
<td>**N-501**</td>
<td>마스킹 정확도 측정 (F-601)</td>
<td>구현 직후 **합성 PII 주입 테스트 100건** (성명·주민번호·계좌·연락처 변형 포함) — **미검출 0건**이 배포 조건. 기획서 10.4 "미측정"을 배포 전 측정 완료로 전환<br/>**✅ 측정 완료 (2026.08.17) — 117건 · 미검출 0건**<br/>· **「미검출」의 정의** — `pii.ts`는 두 겹이다(패턴 마스킹 + 잔존 의심 시 EX-106 전송 차단). 따라서 통과 조건은 **「가려졌거나, 못 가렸으면 차단됐거나」**이고, 둘 다 아닌 것 — 원문이 남았는데 통과까지 한 것 — 만이 미검출이다. 실제로 모델·DB로 새는 경로가 그것뿐이다<br/>· **결과** — 117건 전부 마스킹(차단으로 도망간 건 0). 정상 문장 훼손 0건, 게이트 통과 117/117<br/>· **최초 측정은 미검출 28건이었다.** 전부 성명이며 5가지 유형: 라벨+조사(「이름은 X이고」) · 라벨 누락(「성함은 X」) · 자기소개(「저는 X이라고」) · 역할 라벨(「가입자는 X」) · **라벨 없는 맨이름**(「X이가」)<br/>· **맨이름은 성씨 기반으로 잡되 조건을 세 겹으로 조인다** — ① 어절 **시작** ② 성씨+정확히 2음절 ③ 조사가 **반드시** 붙고 어절이 끝날 때. 느슨하게 하면 참사가 난다(실측: 종**신보험**·해**지환급금**·**설명**을이 이름으로 잡혔고, ③을 선택으로 두자 「전화로」·「원금이」가 통째로 이름이 됐다)<br/>· **⚠️ 과잉 마스킹이 늘 안전하지는 않다** — 초기 규칙이 「어머니가」를 지웠는데, 대리 가입 여부는 판단에 직결되는 사실이다. 가족 호칭·흔한 명사를 예외에 넣고 **정상 문장 훼손 0건**을 테스트로 고정했다. EP-3의 「의심스러우면 가린다」는 사실관계를 지워도 된다는 뜻이 아니다<br/>**⚠️ 「미검출 0건」은 이 세트 안에서만 참이었다 (2026.08.31 · N-406)** — 레드팀 시험이 **이 세트에 없던 변형**에서 실제 구멍을 찾았다: 주민등록번호 **앞자리만 하이픈 없이** 온 경우(「앞자리가 800101인 주민번호예요」)를 가리지도 막지도 않았다. 기존 RRN 문항이 전부 뒷자리를 달고 있어 이 세트로는 잡히지 않던 자리다<br/>**✅ 세트 확대 완료 (2026.08.31) — 117건 → 132건 · 미검출 0건 유지.** 앞 6자리 3값 × 문형 5종을 더했다. 낱말이 숫자 **앞에 오는 경우와 뒤에 오는 경우**를 함께 넣고, 「앞 6자리」처럼 `앞자리` 토큰에 걸리지 않는 표기도 넣어 **규칙이 낱말 하나에만 기대고 있지 않은지** 본다. 이 축은 마스킹이 아니라 **차단(EX-106)으로 통과**한다 — 가려짐 117 · 차단만 15<br/>· **오탐 방어선을 같이 넓혔다** — 6자리 숫자를 그 자체로 의심하면 이 도메인에서 흔한 10만\~99만 원 금액이 통째로 막힌다. 「보험료 350000원」 계열 4문장과 「주민등록증」(트리거 `주민`+`번호`가 아니라 접두사만 같다)을 정상 문장 세트에 넣어 **훼손 0 · 차단 0**을 고정했다<br/>· **가드가 실제로 잡는지 확인했다** — 잔존 의심 규칙을 무력화하면 새 문항 15건이 그대로 미검출로 뜬다. 수정 이전 상태가 재현되므로 이 문항들은 회귀를 실제로 막는다<br/>· 배포 조건의 분모는 여전히 합성 세트다. **세트가 무엇을 놓치는지는 N-406이 따로 잰다** — 편입은 「이번 것을 막았다」이지 「다음 것도 막는다」가 아니다</td>
</tr>
<tr>
<td>**N-502**</td>
<td>마스킹 실패 안전장치</td>
<td>잔존 의심 패턴 감지 시 모델 전송 차단 (EX-106) — 측정 100%와 별개로 런타임 이중 방어</td>
</tr>
<tr>
<td>**N-503**</td>
<td>**동의 기록 미보존 확정 (U-10)**</td>
<td>민감정보 동의(F-602)는 **세션 내 절차 요건으로만 두고 기록을 보존하지 않는다.** 근거: ① 서비스에 저장된 민감정보가 존재하지 않으므로 사후 입증 대상인 '보관 중인 처리'가 없다 ② 동의 기록을 개인과 연결해 보존하려면 식별자가 필요한데, 식별자 도입은 SR-X06(무계정)과 최소수집 원칙을 깨뜨려 더 큰 위험을 만든다 ③ 동의 절차 자체는 정적 화면(S-03 진입 단계)으로 상시 증빙 가능. **운영 개시 후 법률 검토로 재확인한다** — 본 판정은 법률 자문이 아니다</td>
</tr>
<tr>
<td>**N-504**</td>
<td>처리방침 고지</td>
<td>글로벌 푸터에 개인정보 처리방침 — 수집하지 않는 구조를 그대로 서술 (저장 없음 · 마스킹 · 세션 파기 · **남용 방지 목적의 IP 일시 처리 — 24시간 내 자동 소멸, 저장 없음 — 고지 포함**)<br/>**✅ 구현 (2026.08.17)** — `/privacy`, 푸터 링크<br/>**상투구를 쓰지 않았다** — 「필요 최소한으로 수집하며 안전하게 보관합니다」는 이 서비스에서 **거짓말이다**. 애초에 보관하지 않는다. 적힌 내용은 전부 코드에서 확인한 사실이고, 코드가 바뀌면 이 문서도 바뀐다<br/>**외부 전송을 밝힌다** — 마스킹된 진술이 Anthropic(미국)으로, 법령명·조문번호가 법제처로 나간다. 이용자가 알아야 할 사실이라 빠뜨리지 않았다<br/>**고령 이용자 기준으로 썼다** — 「파기」가 아니라 「지웁니다」, 「제3자 제공」이 아니라 「어디로 보내지는지」. 법률 용어가 섞이지 않았는지 테스트가 검사한다<br/>**어긋남을 테스트로 잡는다** — 처리방침이 코드와 어긋나면 보통 문서만 남는다. 명세 요구 항목이 실제로 적혀 있는지, **하지 않는 일을 한다고 쓰지 않았는지**(상투구·없는 기능)를 렌더 결과에서 검사한다<br/>**✅ 확장 스프린트 반영 (2026.08.31)** — 「기기 안에서만 움직이는 것」 절을 신설했다. 읽어주기 음성 · 해피콜 연습에서 고른 답 · 체크리스트 「가지고 있어요」 표시는 전부 클라이언트 상태라 **「저장한다」에도 「받지 않는다」에도 들어가지 않는다.** 그런데 적지 않으면 이용자는 그것이 어디론가 나간다고 읽는다 — 특히 **음성은 마이크를 떠올리게 한다.** 그래서 「저희 서버도 인공지능도 관여하지 않습니다 · 녹음하지 않고 마이크를 쓰지도 않습니다」를 명시하고 렌더 테스트로 고정했다<br/>**⚠️ 시험 세트 크기가 화면에서 어긋나 있었다** — 「가상의 사례 **117건**으로 시험해」가 문자로 박혀 있어, 세트가 132건이 된 뒤에도(N-406 편입) 화면만 117을 말했다. `PII_TEST_SET_SIZE` 상수로 올리고 **실제 세트 크기와 대조하는 테스트**를 붙였다 — DB가 필요 없는 코드 대 코드 대조라 CI에서 늘 돈다. 랜딩 코퍼스 배지(S-01)·적재 로더 note와 **같은 종류의 드리프트**이며, 이것이 화면에 남아 있던 마지막 하나다</td>
</tr>
</table>
## 8. N-6xx 품질 KPI — 기획서 12.3 상속
<table header-row="true">
<tr>
<td>ID</td>
<td>지표</td>
<td>기준</td>
</tr>
<tr>
<td>**N-601**</td>
<td>결론 구간 정확도</td>
<td>**84% (71/85 = 83.5%) 유지** — 재실행 2회 모두 83.5% 재현. 회귀 측정은 검증셋 120건 기준<br/>**⚠️ 제품 기준 재측정 (2026.08.17) — 90% (36/40) · CI [76.9, 96.0]**<br/>위 84%는 **완결된 조정례 결정서를 넣고 위원회 결정을 예측**한 실험(`pilot_law.py`)의 값이고, 제품은 **소비자 진술에서 정제한 사실관계**로 판단한다. 다른 과제다. 실험 수치를 제품 성능으로 걸어 두었던 것을 바로잡고, 제품을 그대로 태운 측정을 새로 했다(검증셋 120건 · API 실패 0 · 형식 오류 0). 실험 수치는 지우지 않는다 — 당시 측정이 수행된 기록이며 화면에 「이전 측정」으로 구분해 싣는다. 원자료는 `measure/`<br/>**커버리지는 33.3% (40/120)** · CI [25.5, 42.2]로 공개값(66~71%)보다 크게 낮다. 「유보는 설계」라는 입장을 유지하고 이 값을 그대로 공개하기로 결정(2026.08.17)<br/>**⚠️ 임계 4의 근거가 제품에서는 성립하지 않는다** — 기획서는 「4점 미만 구간 정확도 57%로 baseline과 구분되지 않는다」를 근거로 임계를 정했는데, 제품 측정에서는 **유보 구간 정확도가 72.5% (58/80) · CI [61.9, 81.1]**로 baseline 50%를 포함하지 않는다. 그럼에도 임계 4를 유지한 이유는 결론 구간 정확도(90%)를 지키기 위해서다. 확신도가 3점 79건·4점 37건으로 몰려 있어 임계를 한 점 내리면 커버리지가 33%→99%로 튀고 정확도는 78%로 내려간다 — **점진 조정이 불가능한 분포**다. 이 사실을 S-06에 함께 공개한다<br/>**✅ v2 재측정 — 공식 런 (2026.08.31 · 검증셋 120건 × 재현 2회 · API 실패 0 · 형식 오류 0)**<br/>**결론 구간 정확도 92.3% (60/65) · CI [83.2, 96.7]** — 회차별 **93.1% (27/29) · 91.7% (33/36)**. 8/17의 90%(36/40)는 **한 번 잰 값**이었고, 이번에도 **약속(90%)은 두 회 모두 위에서 유지**됐다. 표본 합산을 주 수치로 하되 **회차별 값을 항상 병기한다** — 합산 240표본은 120건을 두 번 잰 것이지 독립 240건이 아니다<br/>**재현을 요구하면 값이 달라진다** — 두 회 모두 임계 이상인 사건은 25/120(20.8%)뿐이고 **그 구간 정확도는 96.0% (24/25)**다. 한쪽 회차에서만 결론이 선 사건이 15건(rep1만 4 · rep2만 11). 커버리지를 낮추는 대신 정확도가 오르는 교환이며, S-06이 이 행을 따로 싣는다<br/>**임계 4를 유지하는 근거는 v2에서 바뀌었다 — 세 겹으로.** 바로 위 8/17 관측(기획 단계의 「유보 구간은 baseline과 구분되지 않는다」가 제품에서 성립하지 않음)은 v2에서도 그대로다 — **유보 구간 64.6% (113/175)**, 양측 이항 p = 0.00014. 기획서가 쓴 근거는 제품에서 죽었고, 다음 셋이 그 자리를 대신한다<br/>· **분포가 여전히 점진 조정을 허용하지 않는다** — 확신도 구간별 정확도 2점 58.3%(n=12) · 3점 65.0%(n=163) · **4점 91.9%(n=62)** · 5점 100%(n=3). 3\~4점 사이 26.9%p 단절이 8/17과 같은 자리에 그대로 재현됐다. 임계를 한 점 내리면 **커버리지 27.1%→95.0%, 정확도 92.3%→72.8%**로 뛰고, 한 점 올리면 커버리지 1.2%로 서비스가 성립하지 않는다<br/>· **확신도 변별력은 제품 기준에서도 유의하다** — 결론 구간 대 유보 구간 Fisher 양측 **p = 0.0020(rep1) · 0.0055(rep2)**. 회차별로 각각 유의하므로 합산 p에 기대지 않는다<br/>· **임계를 낮추는 개입을 세 번 실험해 세 번 기각했다** — 자기일관성 합의(R-04) · 확신도 루브릭(R-04 파생) · 약관 주입(R-05). 전부 사전 고정 기준으로 판정했고, **두 기각 실험이 무너진 자리가 같았다**: 원래 유보하던 사건을 결론 내게 만든 신규 커버 구간(76.5% · 66.7%)에서만 틀렸고 기존 결론 구간은 무손상이었다. **유보되는 사건이 실제로 더 어려운 사건**이라는 뜻이고, 임계 4가 임의의 선이 아니라 **난이도 경계에 서 있다는 실측 근거**다 (01-scope 9.1 R-04·R-05)<br/>원자료 `measure/official-rep1.jsonl` · `official-rep2.jsonl` · 기록 `measure/official-measure.md`. **이 런 이후 판단 계층은 동결이다**</td>
</tr>
<tr>
<td>**N-602**</td>
<td>결론 제시 커버리지</td>
<td>**66\~71% 유지** (정확도 희생 없이)<br/>**⚠️ 제품 기준 재측정 (2026.08.17) — 33.3% (40/120) · CI [25.5, 42.2]**<br/>위 66\~71%도 N-601과 같은 pilot 실험의 값이라 제품 성능이 아니다. 「유보는 설계」를 유지하고 33.3%를 그대로 공개한다(2026.08.17 결정). **목표를 66\~71%로 남겨 두면 커버리지를 올리라는 압력이 되어 임계 4를 흔든다** — N-601의 분포 설명대로 임계를 한 점만 내려도 커버리지 33%→99%, 정확도 90%→78%로 뛴다. **회귀 판정 기준은 33.3%다**<br/>**✅ v2 재측정 — 27.1% (65/240) · CI [21.9, 33.0] (2026.08.31 공식 런)**<br/>회차별 **24.2% (29/120) · 30.0% (36/120)**. 8/17의 33.3%보다 낮게 실현됐으나 **새 현상이 아니라 기록된 성질의 재확인**이다 — 단일 런 커버리지는 24\~33%에서 흔들린다(R-04 기준선 25.0% · 루브릭 홀드아웃 기준선 23.9% · 이번 24.2/30.0%). 33.3%는 그 범위의 높은 쪽 실현이었다<br/>**⚠️ 그래서 회귀 판정 기준을 점이 아니라 범위로 바꾼다** — 33.3%를 고정 기준선으로 두면 **정상 변동이 회귀로 오판된다**(24.2%는 33.3% 대비 −9.1%p다). **판정 기준은 「회차별 값이 24\~33% 범위 안이고 결론 구간 정확도가 90% 이상」**이며, 한 번 재서 판정하지 않는다<br/>**목표를 66\~71%로 남겨 두지 않는 이유는 v2에서 더 분명해졌다** — 커버리지를 올리려는 개입 세 건을 사전 고정 기준으로 전부 기각했고, 그중 둘은 **정확히 새로 커버한 구간에서만 틀렸다**(N-601). 커버리지 목표는 임계 4를 흔드는 압력이 된다</td>
</tr>
<tr>
<td>**N-606**</td>
<td>슬롯 추출 정확도 (① 상담)</td>
<td>**✅ 측정 완료 (2026.08.23) — 99.2% (119/120) · CI [95.4, 99.9]** · 날조 **0/31** · CI [0.0, 11.0]<br/>
**신설 이유** — N-601·602는 **판단 계층만** 잰 값이다. `measure-product-live.test.ts`가 `facts_summary`를 `judgeRaw`에 직접 넣고 `channel`을 `"UNKNOWN"` 상수로 두기 때문이다(도구 선택 변동을 섞지 않으려는 의도적 격리). 그래서 **이용자 진술에서 슬롯을 뽑는 구간이 측정 대상 밖**이었고, 「은행 창구 ELS → 방카슈랑스」 결함(F-201, 2026.08.21)이 수치에 전혀 잡히지 않았다<br/>
**세 방향을 따로 센다** — 슬롯은 `search_case`·`analyze_risk_pattern`·`check_documents`의 필터라 틀리는 방향에 따라 해로움이 다르다. **오분류**(단서가 있는데 다른 값 — 엉뚱한 비교군) · **누락**(단서가 있는데 비움 — 되묻기로 회복) · **날조**(단서가 없는데 특정 값 — 가장 해롭다). 하나로 뭉친 정확도만 내면 무엇을 고쳐야 할지 알 수 없다<br/>
**날조 시험에 「유혹」을 넣는다** — 단서가 아예 없는 진술보다 **다른 것을 가리키는 숫자·장소가 문장에 있는** 진술이 위험하다. 민원 접수일·손실 통보일·갱신일·항의하러 간 은행·현재 나이·가족 나이를 넣은 10건에서 전부 비운 채 통과했다<br/>
**⚠️ 이 수치는 하한이 아니라 상한에 가깝다** — 진술 43건을 직접 썼다. 실제 이용자의 말은 더 흐트러져 있고, 쓴 사람이 코드 동작을 알아 난이도가 낮을 수 있다. 조정례 원문을 쓰지 못한 이유는 그것이 완결된 결정서의 서술이지 소비자의 첫 진술이 아니기 때문이다 — 그 차이가 공개 수치를 갈아엎게 만든 문제였다(N-601). 날조 0건도 분모 31이라 CI 상한이 11.0%다. 「없다」가 아니라 「이 범위에서 관측되지 않았다」로 읽는다. 운영 기록이 쌓이면 실제 진술로 다시 잰다<br/>
원자료 `measure/consult-measure.jsonl` · 요약 `measure/consult-measure.md`</td>
</tr>
<tr>
<td>**N-603**</td>
<td>환각</td>
<td>**0%** — N-405 자동 대조로 상시 검증</td>
</tr>
<tr>
<td>**N-604**</td>
<td>유보 후 재상담 전환율</td>
<td>목표값 없이 **측정 개시** (DR-302 익명 카운터) — 목표는 데이터 확보 후 설정</td>
</tr>
<tr>
<td>**N-605**</td>
<td>모델 교체 시</td>
<td>확신도 임계 재보정 + N-601·602 재측정 완료 전 배포 금지 (A-2·A-6)</td>
</tr>
</table>
## 9. N-7xx 호환성
<table header-row="true">
<tr>
<td>ID</td>
<td>항목</td>
<td>요구사항</td>
</tr>
<tr>
<td>**N-701**</td>
<td>브라우저</td>
<td>모바일: iOS Safari · Chrome · Samsung Internet 최신 2개 버전 / 데스크톱: Chrome · Edge · Safari 에버그린. **카카오톡 인앱 브라우저에서 열람 가능해야 한다** — 기획서 7.2 "가족이 카카오톡으로 링크를 보내주는 경로"<br/>**◐ 부분 실측 (2026.08.21) — iOS WKWebView 재현, 카카오톡 자체는 미검증**<br/>시뮬레이터에는 App Store가 없어 카카오톡을 설치할 수 없다. 대신 **카톡 iOS 인앱 브라우저의 실체인 `WKWebView`**를 같은 UA(`KAKAOTALK 10.5.0`)로 띄워 전 구간을 태웠다(iPhone 17 · iOS 26 시뮬레이터).<br/>· **랜딩·상담·동의·판단·검증 결과 전부 정상 렌더**. 가로 스크롤 없음, 업권별 편차는 카드 구조라 넘치지 않는다<br/>· **판단 스트리밍이 WKWebView에서 버퍼링 없이 도달** — 경과 시간 9초→41초→61초로 증가, 단계가 하나씩 쌓이며 약 100초 뒤 결과 화면 도달. **여기가 가장 깨지기 쉬운 지점이었고 통과했다**<br/>· **화면 전환 시 제목에 포커스가 실제로 이동**하는 것을 눈으로 확인(A11Y-7)<br/>· **⚠️ 백그라운드 전환은 이 시험으로 증명되지 않는다** — 판단 도중 홈 버튼으로 50초 내린 뒤 복귀했을 때 스트림이 살아 있었으나, **시뮬레이터는 실기와 달리 백그라운드 정지를 강제하지 않는다.** 실기에서는 iOS가 유예 후 네트워크를 멈춰 끊길 수 있다<br/>**✅ 시뮬레이터·에뮬레이터 전 구간 재주행 (2026.08.24) — 결함 2건을 여기서 찾았다**<br/>· **백그라운드 서스펜드를 강제해 재현했다** — 앱과 WebContent 프로세스를 90초간 `SIGSTOP`으로 얼렸다 풀었다(시뮬레이터는 백그라운드만으로는 정지하지 않는다). **스트림이 살아남아 복귀 직후 결과 화면이 떴다**<br/>· **결함 — 결과 화면 새로고침이 판단을 말없이 재실행했다** (T-5 위반). 90초·모델 호출·N-203 일일 상한을 설명 없이 쓴다 → 수정<br/>· **결함 — 가입 연월 칸을 누르기만 해도 이번 달이 채워졌다** (화면 명세 S-03 되묻기). 기준일이 적용 법령을 가르므로 다른 법으로 판단할 수 있었다. `max` 속성은 iOS가 보지 않아 코드에서 다시 걸렀다 → 수정<br/>· 그 밖 확인 — 상담·되묻기·판단·결과·근거 상세·뒤로가기·가입 전 확인·검증 결과 전부 정상. PII 마스킹 통과(연락처 포함 진술) · 키보드가 「보내기」를 가리지 않음 · 입력창 글꼴 18.9px로 iOS 자동 확대 미발생 · 핀치 확대 동작(WCAG 1.4.4 통로)<br/>· **안드로이드도 쟀다** — Android 에뮬레이터(API 36) Chrome에서 판단 스트림 실측: 첫 이벤트 **2.5초**, 하트비트 10초 간격 오차 0.2초, 88.9초 완주, `content-encoding: br`에도 버퍼링 없음. 카톡 안드로이드 인앱은 Chrome WebView라 같은 Blink 엔진이다<br/>**✅ 실기 완료 — 배포 게이트 통과 (2026.08.24, iPhone · iOS · 카카오톡 인앱 · 이동통신망)**<br/>· **1회차 완주** — 랜딩→동의→진술→되묻기(가입 연월)→판단→**유보 결과**→근거 상세→뒤로가기 전부 정상<br/>· **2회차 백그라운드 1분** — 복귀 시 **「연결이 끊겼습니다」+「다시 시도하기」**. **「다시 시도하기」로 진술을 다시 묻지 않고 판단이 재개돼 결과 화면까지 도달**<br/>· **⚠️ 실기에서는 iOS가 실제로 연결을 끊는다** — 시뮬레이터에서는 `SIGSTOP`으로 프로세스를 90초 얼려도 스트림이 살아남았는데(네트워크는 그대로였다), 실기에서는 끊겼다. 8/21부터 남아 있던 「시뮬레이터로 증명되지 않는다」가 **끊긴다는 쪽으로 판명**됐다<br/>· **→ 즉, 8/24에 만든 재시도 경로(EX-302)가 없었으면 이 자리에서 떨어졌다.** 원래는 「상담 다시 시작하기」뿐이라 진술부터 다시 써야 했다<br/>· 가입 연월 칸에서 **버튼에 고른 값이 표시되는 것**(「2019년 5월로 답변하기」)과 안내 문장이 붙은 것을 실기에서 확인<br/>**실기에서 나온 UX 지적 2건 — 고쳤다 (#83·#84)**<br/>· **근거 카드가 길어 「다음에 하실 일」의 신청권·1332(F-306)까지 내려가기 힘들다** → 조문 원문·사건 요지를 접이식으로, 제목 아래 「다음에 하실 일 바로 보기」 앵커 추가. 375px에서 16.1화면 → 12.4화면, 점프는 한 번<br/>· **실행 로그가 코드로 보인다** → S-08 표현 규칙(「기술 로그 원문이 아니라 일반 사용자가 읽을 수 있는 문장으로, 도구 원어명은 보조 표기」)을 지키지 않고 있었다. 도구 5종×결과 4종에 문장을 달고 키·값을 한국어로 옮겼다<br/>**남은 것 — 안드로이드 실기.** 에뮬레이터 Chrome은 쟀으나(첫 이벤트 2.5초·88.9초 완주) 카톡 안드로이드 인앱 자체는 미검증이다. Chrome WebView라 같은 엔진이지만 인앱 껍데기의 동작은 다를 수 있다<br/>**✅ 실기 전 사전 점검 (2026.08.24)** — 실기에서 왕복이 길어지지 않게 미리 잡을 수 있는 것을 잡았다<br/>· **프로덕션 판단 스트림 실측** — 카톡 iOS UA로 `precase.vercel.app`에 봉인 세션을 넣어 전 구간 1회. 첫 이벤트 **1.6초**(N-102 목표 5초 안), 하트비트 10초 간격이 **오차 0.1초로 도달**, 총 90.3초 14청크로 완주<br/>· **⚠️ Vercel 엣지가 `content-encoding: br`을 붙인다** — `Cache-Control: no-transform`은 캐시 지시어라 압축을 막지 못한다. **다만 청크별로 flush돼 버퍼링은 없었다**(39바이트 하트비트가 제때 도착). 헤더가 목적을 달성하지 못하는데도 결과가 맞는 상태이므로, 이 실측 없이는 안전하다고 말할 수 없었다<br/>· **백그라운드 복귀 경로를 코드로 열었다** — 스트림이 끊겨도 봉인 세션이 남아 「다시 시도하기」로 이어진다 (EX-302). 인앱에서 `sessionStorage`가 막혀도 소프트 내비게이션 구간은 메모리 사본으로 건넌다. 상담 한 턴이 끊긴 경우도 진술을 다시 쓰지 않고 재전송된다<br/>· **375px 전 화면 재확인** — 가로 스크롤 0, 터치 영역 48px 미만 0(문장 속 인라인 링크 제외 — WCAG 2.5.8 예외)</td>
</tr>
<tr>
<td>**N-702**</td>
<td>뷰포트</td>
<td>360px\~ 모바일 우선 (화면 1장). JS 미지원 환경은 범위 외</td>
</tr>
<tr>
<td>**N-703**</td>
<td>접근성</td>
<td>화면 4.2 A11Y-1\~7 상속 — 배포 전 Lighthouse 접근성 **90점 이상**<br/>**✅ 측정 완료 (2026.08.17) — 8개 화면 전부 100점** (프로덕션 실측, 원자료 `measure/lighthouse/`)<br/>랜딩 · 가입 전 확인 · 검증 결과 · 상담 · 데모 목록 · 데모 3종<br/>**⚠️ 점수만으로는 부족하다** — Lighthouse는 **10개 항목을 「사람이 확인」으로 남기고 점수에 넣지 않는다**(포커스 이동·탭 순서·상태 표시 등). 자동 검사 통과 23건은 그 밖의 것이다<br/>**직접 잰 것** — 본문 18px · 48×48px 미만 터치 영역 **0개** · 16px 미만 본문 **0개** · 가로 스크롤 없음 · 처리 중 `aria-live` 동작<br/>**⚠️ 직접 재다가 결함을 찾았다** — 화면 전환(진술→되묻기, 처리중→판단 결과)이 페이지 이동이 아니라 상태 변화라, 제목은 바뀌는데 **포커스가 이전 화면에 남았다.** 눈으로 보는 이용자에게는 명백하지만 스크린리더 이용자에게는 아무 일도 일어나지 않는다 — 주 사용자가 고령층이라 이 차이가 실제로 사람을 막는다. 제목에 `tabIndex={-1}`을 주고 상태가 바뀔 때 포커스를 옮기도록 고쳤다(A11Y-7). **Lighthouse 100점에서는 드러나지 않던 결함이다**<br/>**✅ 추가 측정 (2026.08.26) — 기관 화면 4종·허브·MCP 안내까지 6개 화면도 전부 100점**(결함 0). 소비자 화면과 같은 기준을 새로 더한 화면에도 적용했다<br/>**✅ 게이트 1차 재측정 (2026.08.31) — 8개 화면 전부 100점 · 실패 0** (로컬 프로덕션 빌드 `next build` → `next start`). 랜딩 · 사전 점검 · 상담 · 검증 결과 · **해피콜(신규 S-14)** · 데모 3종. **프로덕션 실측은 9/5 배포 후 다시 돌린다** — 이 측정은 로컬 빌드다<br/>**확장 스프린트에서 수동 검사가 잡은 것 (자동 점수 100점 상태에서 나왔다)** — 근거 각주 앵커 **21px** · PDF 저장 안내 토글 **31px**. 둘 다 2026.08.30 수정(앵커는 시각 디자인을 두고 가상요소로 히트 영역만 49px로 확장, 앵커 상하 13px 탭 명중을 실브라우저로 확인). 해피콜·체크리스트 보유 표시 등 신규 요소는 설계 단계에서 48px를 지켜 **미달 0**<br/>**관찰(미수정)** — 실행 로그 「경과」 줄 17.1px. A11Y-1은 본문(body) 기준이라 위반은 아니다</td>
</tr>
</table>
## 10. N-8xx 유지보수·문서 정합
<table header-row="true">
<tr>
<td>ID</td>
<td>항목</td>
<td>요구사항</td>
</tr>
<tr>
<td>**N-801**</td>
<td>열거·수치 동시 갱신</td>
<td>슬롯 열거(기능 2장) 변경 시 데이터·DB 명세서 동시 갱신. 검증 수치 변경 시 DR-106 · S-06 · 기획서 3.3 동시 갱신 (Q-3)</td>
</tr>
<tr>
<td>**N-802**</td>
<td>초기값 조정 절차</td>
<td>N-2xx 상한·N-1xx 목표의 조정은 **운영 측정 근거 + 본 문서 갱신**을 동반해야 한다. 코드에만 바꾸고 문서를 방치하지 않는다</td>
</tr>
<tr>
<td>**N-803**</td>
<td>측정 산출물 보존</td>
<td>모든 측정의 스크립트·입력·결과를 버전 관리에 보존 — 기획서 10.4 "원 산출 스크립트 미보존" 재발 방지</td>
</tr>
<tr>
<td>**N-804**</td>
<td>범위 동결</td>
<td>**09.03 이후 범위·수치 변경 금지** (범위 문서 9장). 이후는 안정화·제출 준비만</td>
</tr>
</table>
---
<callout icon="📌" color="gray_bg">
	**다음 문서** — 용어 정의 요구사항 (마지막). 범위\~비기능 8개 문서에서 등장한 용어·ID 체계를 걷어 올려 단일 사전으로 고정한다. 남은 미결은 **U-9(쟁점 태그 열거 — DB 명세서에서 확정)** 하나다.
</callout>
