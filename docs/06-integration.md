<!-- 원본: https://app.notion.com/p/3ba0f70dd29c80e09659ef7174bb4b47 · export 2026-08-15 · 정본은 노션, 이 파일은 참조 사본 -->
# 외부 연동 요구사항
프리케이스(PreCase) — 2026 금융 AI Challenge 1차 심사 제출본
<callout icon="🎯" color="blue_bg">
	**이 문서의 역할** — 범위 문서 SR-601\~606의 외부 연동을 **엔드포인트·인증·캐시·폴백·알려진 결함 수준**으로 확정한다. 프리케이스의 외부 의존은 단 두 곳 — **법제처 국가법령정보 OPEN API와 Claude API**다. 이 좁은 표면이 심사 기간 무중단(C-2)의 전제이므로, 연동마다 **실패 시 동작을 반드시 정의**한다.
	장애 시나리오별 사용자 경험은 예외처리 요구사항이 상속한다.
</callout>
## 1. 문서 정보
<table header-row="true">
<tr>
<td>항목</td>
<td>내용</td>
</tr>
<tr>
<td>문서명</td>
<td>외부 연동 요구사항</td>
</tr>
<tr>
<td>상위 문서</td>
<td>서비스 범위(SR-6xx) · 기능(F-5xx) · 데이터(DR-104·2xx) 요구사항</td>
</tr>
<tr>
<td>작성 기준일</td>
<td>2026.08.14</td>
</tr>
<tr>
<td>E-ID 체계</td>
<td>E-01\~03 법제처 연동 · E-04\~05 비연동 대체 경로 · E-06 Claude API</td>
</tr>
</table>
## 2. 연동 총괄 맵
**어떤 기능이 외부에 의존하고, 어떤 기능이 의존하지 않는지**를 먼저 확정한다.
<table header-row="true">
<tr>
<td>도구 / 기능</td>
<td>외부 연동</td>
<td>내부 자산</td>
</tr>
<tr>
<td>`lookup_statute` (F-501)</td>
<td>**E-01** 현행법령 (이관 삭제 조문 외 전부 — 시점 무관 법령 포함)</td>
<td>**E-05 경로**: DR-104 스냅샷 (기준일 \< 2021.3.25) · DR-201 캐시</td>
</tr>
<tr>
<td>`search_precedent` (F-502)</td>
<td>**E-02** 판례</td>
<td>DR-202 캐시</td>
</tr>
<tr>
<td>법령해석례 보조 조회</td>
<td>**E-03** 법령해석례</td>
<td>DR-202 캐시</td>
</tr>
<tr>
<td>`search_case` (F-503)</td>
<td>**없음**</td>
<td>DR-101 조정례 388건</td>
</tr>
<tr>
<td>`check_documents` (F-504)</td>
<td>**없음**</td>
<td>DR-102 약관 266건 (E-04 대체 경로의 결과물)</td>
</tr>
<tr>
<td>`analyze_risk_pattern` (F-505)</td>
<td>**없음**</td>
<td>DR-105 사전 집계</td>
</tr>
<tr>
<td>에이전트 4계층 (F-3xx)</td>
<td>**E-06** Claude API</td>
<td>—</td>
</tr>
</table>
<callout icon="💡" color="gray_bg">
	**도구 5종 중 3종은 외부 무의존이다.** 법제처 API가 완전히 죽어도 조정례 검색·자료 진단·패턴 집계는 동작하며, 법령·판례는 캐시·스냅샷으로 폴백한다. 완전 불능 조건은 **E-06(Claude API) 단일 장애**뿐이며 그것도 **판단 축에 한정**된다 — 예방 축(모델 미사용, 기능 4장)과 데모 3종(F-701)·검증 결과는 정상이다.
</callout>
## 3. 법제처 국가법령정보 OPEN API — E-01\~03
**공통 사항**
<table header-row="true">
<tr>
<td>항목</td>
<td>내용</td>
</tr>
<tr>
<td>제공처</td>
<td>법제처 국가법령정보 공동활용 ([open.law.go.kr](http://open.law.go.kr))</td>
</tr>
<tr>
<td>인증</td>
<td>인증키 `OC=precase` (자동승인 완료) — 쿼리 파라미터 방식. **⚠️ 인증키만으로는 부족하다 — 아래 「Referer 검증」 필수**</td>
</tr>
<tr>
<td>형식</td>
<td>**JSON** (신청 시 목록·본문 모두 JSON 선택 완료)</td>
</tr>
<tr>
<td>호출 주체</td>
<td>MCP 도구 서버만 — 클라이언트(브라우저)에서 직접 호출 금지. 인증키가 클라이언트에 노출되면 안 된다</td>
</tr>
<tr>
<td>**⚠️ Referer 검증 (필수)**</td>
<td>법제처는 호출자를 **`Referer` 헤더가 신청 시 등록한 도메인과 일치하는지**로 검증한다. **모든 요청에 `Referer: https://precase.vercel.app/`를 붙여야 한다.** 없으면 `"필수입력요소 검증에 실패하였습니다"`라는 **오해를 부르는 에러**가 돌아온다(파라미터 문제로 보이지만 실제 원인은 Referer 부재다). 2026.08.16 실측 확정 — 헤더 유무만으로 실패/성공이 갈리는 것을 대조 확인했다.<br>**IP 검증이 아니다** → Vercel 서버리스의 동적 출구 IP가 문제되지 않는다(신청서의 「서버장비 IP주소」는 공란으로 둔다). 다만 **배포 도메인이 바뀌면 신청 정보의 도메인도 함께 바꿔야 한다**</td>
</tr>
<tr>
<td>쿼터</td>
<td>공표된 호출 한도 없음 — 단정하지 않고 **캐시(DR-201·202)로 호출량 자체를 최소화**한다</td>
</tr>
</table>
### E-01 현행법령 조문 조회
<table header-row="true">
<tr>
<td>항목</td>
<td>정의</td>
</tr>
<tr>
<td>용도</td>
<td>`lookup_statute` — 현행 조문 원문·시행일자. **시점 무관 법령(상법·민법·약관규제법·할부거래법, 보험업법 제95조의2 존치 항)은 기준일과 무관하게 이 경로**다. 금소법 조문은 기준일 ≥ 2021.3.25일 때</td>
</tr>
<tr>
<td>확인된 범위</td>
<td>상법(1,184개 항목)·민법·약관규제법·자본시장법·할부거래법·보험업법·금소법 등 **8종 조회 확인 완료** (기획서 8.2).<br>**⚠️ 법령 목록 검색도 부분일치다** — 판례(E-02)와 같은 결함이 여기에도 있다. 「상법」 조회 시 56건이 걸리는데 **정확 일치가 34번째**라 기본 20건 수신으로는 찾지 못한다(가나다순 정렬). **`display=100`으로 전량 받아 법령명이 정확히 같은 항목을 고른다.** 정확 일치가 없으면 엉뚱한 법령의 조문을 반환하지 않고 실패시킨다(EP-3). 2026.08.16 실측</td>
</tr>
<tr>
<td>캐시</td>
<td>DR-201 — 키: 법령명+조문번호+기준일, TTL 1일</td>
</tr>
<tr>
<td>폴백 체인</td>
<td>API 실패 → 캐시(TTL 내) → **폴백 스냅샷 + 최종 확인 시점 표시**(F-702) → 그래도 없으면 해당 근거 없이 진행하며 확신도에 반영 → 유보 가능</td>
</tr>
<tr>
<td>응답 검증</td>
<td>요청한 법령명·조문번호와 응답 일치 확인. 반환값은 F-606 필터 통과 후 자료 블록으로만 사용</td>
</tr>
</table>
### E-02 판례 조회
<table header-row="true">
<tr>
<td>항목</td>
<td>정의</td>
</tr>
<tr>
<td>용도</td>
<td>`search_precedent` — 사건번호 정확 대조 방식의 대법원 판례 원문 (본문 15,978자 수신 확인 완료)</td>
</tr>
<tr>
<td>⚠️ 알려진 결함</td>
<td>**검색이 부분일치로 동작한다** — 요청 사건번호가 **1순위로 오지 않는다**. `2010다76368`(우리파워인컴펀드) 조회 시 무관한 사건이 상위 반환 (기획서 6.6).<br>**⚠️ 2026.08.16 재조사로 결함의 성격이 정정됐다 — 「반환되지 않는다」가 아니라 「1순위로 오지 않는다」였다.** 요청 사건번호는 **결과 목록 안에 실제로 존재**한다. 실측 3건 모두 목록 끝에서 발견: `2010다76368` 16/16건(대법원 2011.07.28 손해배상(기)) · `2016다212272` 5/5건(2018.06.15) · `2019다224238` 4/4건(2023.11.30).<br>→ **대응: 1순위가 아니라 결과 목록 전체를 훑어 정확 일치를 찾는다**(`display=100`). 사건번호 조회는 결과가 4\~16건 규모라 1회 호출로 전량 수신된다. 위치가 항상 끝이라는 보장은 없으므로 **끝 항목을 집지 말고 전량 스캔**할 것. 전량 스캔에도 없으면 그때 표시 불가 플래그. 따라서 **모든 반환 결과에 사건번호 정확 대조를 강제**하고, 불일치면 표시 불가 플래그 (F-502)</td>
</tr>
<tr>
<td>재시도</td>
<td>사건번호 대조 실패 시 **쟁점 키워드 검색으로 1회 재시도** — 재시도 결과도 동일한 대조를 거친다</td>
</tr>
<tr>
<td>캐시</td>
<td>DR-202 — **캐시 적중이어도 정확 대조는 항상 수행** (DR-202 규칙)</td>
</tr>
</table>
### E-03 법령해석례 조회
<table header-row="true">
<tr>
<td>항목</td>
<td>정의</td>
</tr>
<tr>
<td>용도</td>
<td>법제처 유권해석 — **보조 근거 전용.** 단독으로 결론의 근거가 되지 않는다</td>
</tr>
<tr>
<td>정책</td>
<td>E-02와 동일 캐시·필터. 실패 시 재시도 없이 생략 — 보조 근거 부재는 유보 사유가 아니다</td>
</tr>
</table>
## 4. 비연동 대체 경로 — E-04\~05
API로 해결할 수 없어 **설계로 대체한** 두 경로다. 연동처럼 보이지만 런타임 외부 호출이 없다.
<table header-row="true">
<tr>
<td>ID</td>
<td>불가 항목</td>
<td>대체 경로</td>
<td>요구사항</td>
</tr>
<tr>
<td>**E-04**</td>
<td>행정규칙(표준약관) — 인증 범위 밖</td>
<td>**DR-102 약관 조항 266건** (분쟁 사건에서 추출)</td>
<td>런타임 조회 대상은 DR-102뿐. 표준약관 전문이 필요한 기능을 만들지 않는다 — "실제로 분쟁이 된 조항만 모인 추출물이 서비스 용도에 더 적합"(기획서 8.1)</td>
</tr>
<tr>
<td>**E-05**</td>
<td>시점별 연혁 법령 — 조회 불가</td>
<td>**DR-104 삭제 조문 원문 스냅샷 + 시행일 매핑 표** (기획서 6.4)</td>
<td>**요청 조문이 아래 이관 삭제 조문이고** 기준일 \< 2021.3.25이면 `lookup_statute`가 API 대신 스냅샷을 반환하되(그 외 법령은 기준일 무관 E-01 현행 조회) **출처·확인 시점 필수 표기.** 스냅샷 대상: 자본시장법 제46조·제46조의2·제47조·제49조, 보험업법 제95조의2 이관 항·제95조의3. **국가법령정보센터 연혁법령에서 수동 확보하며, 확보 일자·경로를 메타로 기록** (DR-104). 삭제 조문은 개정되지 않으므로 갱신 절차 불요</td>
</tr>
</table>
## 5. Claude API — E-06
<table header-row="true">
<tr>
<td>항목</td>
<td>정의</td>
</tr>
<tr>
<td>용도</td>
<td>에이전트 4계층 전부 (①상담 ②조사 ③판단 ④안내) — 계층 간 컨텍스트 격리 호출</td>
</tr>
<tr>
<td>모델</td>
<td>**운영: Claude Opus 5** (실측 기준 모델). 대조 실험 보관용: Sonnet 5</td>
</tr>
<tr>
<td>호출 구조</td>
<td>계층별 독립 호출 — 6장 권한 매트릭스(P-4xx)에 따라 **계층별 페이로드에 허용된 데이터만 포함** (P-401). MCP 도구 호출은 ② 조사 계층에서만</td>
</tr>
<tr>
<td>비용 통제</td>
<td>**하드 쿼터 설정** (C-4 예산). 세션당 도구 호출 상한(F-607)·되묻기 상한(U-8)·IP 레이트 리밋(U-11)과 함께 4중 방어 — 수치는 비기능 요구사항</td>
</tr>
<tr>
<td>모델 교체</td>
<td>교체 시 **확신도 임계 재보정 필수** (A-2·A-6). 임계값은 설정으로 관리 (F-305)</td>
</tr>
<tr>
<td>장애 시</td>
<td>판단 파이프라인 전체 불능 → 상담 진입을 막고 정직하게 안내 + **데모 3종은 계속 열람 가능**(F-701·DR-107). 상세 시나리오는 예외처리 요구사항</td>
</tr>
<tr>
<td>키 관리</td>
<td>환경변수로만 주입. 저장소·클라이언트 노출 금지</td>
</tr>
</table>
## 6. 공통 연동 규칙
<table header-row="true">
<tr>
<td>#</td>
<td>규칙</td>
</tr>
<tr>
<td>**EC-1**</td>
<td>**타임아웃·재시도 초기값** — 법제처 API: 타임아웃 10초, 멱등 조회에 한해 1회 재시도. Claude API: SDK 기본 + 스트리밍. 초기값이며 운영 측정 후 비기능 요구사항에서 조정</td>
</tr>
<tr>
<td>**EC-2**</td>
<td>**모든 외부 반환값은 F-606 필터를 거친다** — 외부 응답은 지시가 아니라 자료다. 법제처 응답도 예외 없음 (기획서 9.4 도구 반환값 인젝션)</td>
</tr>
<tr>
<td>**EC-3**</td>
<td>**출처·시점 동반 반환** — 외부에서 온 모든 근거는 출처와 확인 시점을 달고 반환된다 (F-501). 폴백 데이터는 폴백임이 표시된다 (F-702)</td>
</tr>
<tr>
<td>**EC-4**</td>
<td>**자격증명 서버 한정** — `OC` 키·Claude API 키 모두 서버 환경변수. 클라이언트 번들·저장소·로그에 노출 금지</td>
</tr>
<tr>
<td>**EC-5**</td>
<td>**신규 외부 연동 금지** — 본 문서에 없는 외부 호출을 추가하려면 범위 문서 9장 변경 통제를 거친다 (SR 5.6 "법제처 외 외부 API 연동은 1차 범위에 없다")</td>
</tr>
</table>
## 7. 준비·점검 체크리스트 (개발 착수 전)
<table header-row="true">
<tr>
<td>#</td>
<td>항목</td>
<td>시점</td>
</tr>
<tr>
<td>1</td>
<td>~~법제처 인증키 신청 정보 점검~~ — **✅ 완료(2026.08.16).** 도메인 등록 위치는 마이페이지가 아니라 **[OPEN API] → [OPEN API 신청]** 화면이었다(마이페이지에는 도메인 란이 없다). 등록값 `precase.vercel.app` · 활용목적 오탈자 정비 · 법령종류 체크(현행법령·판례·법령해석례 × 목록/본문 JSON) · 자동승인. **3종 전부 실호출 검증 완료**</td>
<td>배포 직후</td>
</tr>
<tr>
<td>2</td>
<td>DR-104 스냅샷 확보 — 국가법령정보센터 연혁법령에서 대상 조문 6건 수동 확보 + 메타 기록</td>
<td>1주차 (F-501 구현 전)</td>
</tr>
<tr>
<td>3</td>
<td>~~E-02 정확 대조 로직의 대조 성공률 측정~~ — **✅ 측정 완료 (2026.08.16).** 코퍼스 인용 판례 상위 30종 대상.<br>**대조 성공률 80.0%(24/30).** 정확 일치가 1순위였던 경우는 16.7%(4/24)뿐이며 순위 중앙값 4·최대 16이었다. **1순위만 봤다면 성공률 13.3%** — 전량 스캔(`display=100`)이 **+66.7%p**를 회복한다.<br>미발견 6건 중 4건은 법제처 판례 DB에 **결과 0건**으로 애초에 없다(2016다3638·2013다205457·2014다60608·2001다10458). 도구 결함이 아니라 원천 수록 범위의 한계다</td>
<td>도구 구현 직후</td>
</tr>
<tr>
<td>4</td>
<td>Claude API 하드 쿼터·알림 설정</td>
<td>1주차</td>
</tr>
<tr>
<td>5</td>
<td>폴백 스냅샷 초기 적재 — 핵심 법령 8종의 주요 조문 사전 캐시 (심사 기간 장애 대비)</td>
<td>배포 전</td>
</tr>
<tr>
<td>6</td>
<td>데모 B 인용 대상(홍콩 H지수 ELS 조정결정)이 색인 388건에 **의결번호와 함께 포함되어 있는지** 확인 — 없으면 데모 B 근거 구성 변경</td>
<td>적재 직후</td>
</tr>
</table>
## 8. 이관
<table header-row="true">
<tr>
<td>항목</td>
<td>이관처</td>
</tr>
<tr>
<td>EC-1 타임아웃·재시도 확정값 · E-06 쿼터 수치</td>
<td>비기능 요구사항 (U-4·U-11과 함께)</td>
</tr>
<tr>
<td>연동 장애별 사용자 경험 시나리오</td>
<td>**예외처리 요구사항** — 본 문서의 폴백 체인을 화면 상태(4.3)와 연결</td>
</tr>
<tr>
<td>E-02 대조 성공률 측정 결과 반영</td>
<td>도구 구현 후 본 문서 갱신</td>
</tr>
</table>
---
<callout icon="📌" color="gray_bg">
	**다음 문서** — 예외처리 요구사항. 본 문서의 폴백 체인 + 화면 4.3 공통 상태 + 기능의 유보·되돌림 경로를 실패 지점별 시나리오로 통합한다.
</callout>
