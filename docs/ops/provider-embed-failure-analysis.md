# B-EMBED-01 v2 실패 원인 분해

## 1. 자료와 방법

- 대상: `B-EMBED-01` v2 첫 main Live run `33870910880`, 시험 SHA `8f300efb45e1a758922355fe46a20f76c4a855b4`, job `101016419063`, fixture Blob `36f6897666a18bee64417bbfd83cebefd0cd4c7c`
- 입력: `evidence/diagnostics/B-EMBED-01/33870910880.json`의 질문 100건 순위 원장과 `.github/fixtures/provider-embed-v2.json`의 고정 qrels
- 방법: 저장소 정본 모듈 `.github/scripts/provider-embed-evaluation.mjs`의 `loadEmbedFixtures`·`scoreRankings`에 원장을 그대로 넣어 재계산하고, unit ID를 fixture의 facet·가족·문서와 다시 연결했다.

재계산 결과는 원장 값과 같다. Recall@5 `0.888`, 위험 핵심 Recall@5 `0.9333333333333333`, Precision@5 `0.728`, 정답 칸 `364/500`이다.

이 문서는 실패한 실행의 분석이다. Artifact·digest·채택 provenance가 없으므로 어떤 수치도 `PASS` Evidence가 아니다. Gate 상태를 바꾸지 않는다. 인용한 합성 문장은 평가용이며 실제 상품 사실이나 공식 Snapshot이 아니다.

## 2. 표본 구성이 만드는 제약

v2 Gate는 20가족이고 각 가족은 문서 7개를 가진다. 질문은 가족당 5개이며 모든 질문에 hard negative가 정확히 2개 지정돼 있다.

| 질문 유형 | 수 | 관련 unit | hard negative | 가족 내 나머지 |
|---|---:|---:|---:|---|
| 다중 항목 | 80 | 5 | 2 | 없음 (5 + 2 = 7) |
| 단일 사실 | 20 | 1 | 2 | 4 |

다중 항목 질문에서 가족 문서 7개는 정답 5개와 hard negative 2개로 정확히 나뉜다. 따라서 top 5가 만점이 되려면 **가족의 hard negative 2개를 모두 5위 밖으로 밀어내는 것 외에 다른 경로가 없다.** 이 slice의 측정 대상은 일반적인 검색 능력이 아니라 지정된 적대적 문서의 거부율이다.

같은 이유로 다중 항목 slice에서 Precision@5는 Recall@5와 항상 같은 값이다. 관련 unit 수가 5이고 top-k도 5이므로 `sum(hit)/(5 × 질문 수)`와 질문별 `hit/5`의 평균이 일치한다. 실제 이번 실행의 다중 항목 slice도 두 값이 모두 `0.860`이었다. 전체 집계에서 두 지표를 독립된 두 축으로 읽으면 안 된다.

Precision 상한 `0.84`와 통과에 필요한 `400/500`은 이미 [품질 평가 계획](quality-evaluation-plan.md) 4절에 기록돼 있다. 여기에 더해 확인된 사실은 다음과 같다. 단일 사실 질문 20개가 만점이라고 가정하면 Recall@5 `0.90`은 정답 칸 `370`, Precision@5 `0.80`은 정답 칸 `400`을 요구한다. **v2에서는 Precision 선이 Recall 선보다 30칸 더 엄격하다.**

가족별 Precision `0.80` 조건은 더 엄격하다. 한 가족의 관련 칸 상한은 `4 × 5 + 1 = 21`이고 분모는 `25`이므로, 가족 하나가 놓칠 수 있는 정답 칸은 **최대 1개**다.

## 3. 실패의 구성

### 3.1 단일 사실과 다중 항목의 분리

| slice | 질문 | Recall@5 |
|---|---:|---:|
| 단일 사실 | 20 | **1.000** |
| 다중 항목 | 80 | 0.860 |

단일 사실 질문은 전부 맞혔다. 하나의 사실을 묻는 질의에서 정답 문서를 top 5 안에 넣는 능력은 이번 실패의 원인이 아니다.

다중 항목 80건의 분포는 5/5가 33건, 4/5가 38건, 3/5가 9건이다. 전멸한 질문은 없다.

### 3.2 top 5에 들어온 비관련 문서 136칸

| 구분 | 칸 |
|---|---:|
| 단일 사실 질문의 구조적 여유 칸 (정답이 1개뿐) | 80 |
| 다중 항목 질문의 실제 침입 | 56 |

다중 항목 침입 56칸의 출처는 다음과 같다.

| 출처 | 칸 | 비고 |
|---|---:|---|
| 같은 가족의 선언된 hard negative | **53** | 고유 unit 26개 |
| 같은 가족의 다른 facet | 0 | 구조상 존재할 수 없음 |
| 다른 가족 | 3 | 전체 500칸의 0.6% |

**실패는 corpus 전체의 잡음이 아니라 설계된 적대적 문서에 집중돼 있다.** 168개 문서에서 다른 가족 문서가 top 5로 들어온 것은 500칸 중 3칸뿐이다. 임베딩은 올바른 시나리오 가족을 안정적으로 찾고 있고, 가족 내부에서 대상·시점을 가르지 못한다.

### 3.3 침입한 hard negative와 누락된 정답의 성격

침입한 hard negative는 모두 `다른 대상·시점 또는 요청하지 않은 사실` facet이며, 반복 침입한 문서는 다음 유형이다.

- 이름이 거의 같은 다른 기관: `누리저축은행 Nuri Savings Bank NS-21`이 `누리은행 NR-21` 질의에 침입 (4회)
- 대체된 과거 버전: `세온은행 늘봄신용 SE-6 V2`가 `SE-6 V1` 질의에 침입 (4회)
- 만료된 시점 자료: `이어봄대출 OB-7 V2` 만료 조건이 `OB-7 V3` 질의에 침입 (3회)
- 과거 금리표: `보탬신용 GA-4의 2025년 금리표`가 2026년 9월 질의에 침입 (3회)
- 인접 상품·다른 상품군: `누리투자 NR-I1 디딤펀드`, `미소적금 MS-S11` (각 2~3회)

가장 자주 누락된 정답은 대칭적으로 현재 시점의 수치 문서다. `이어봄대출 OB-7 V3 한도 1800만원` (4회), `보탬신용 GA-4 기본금리 연 7.3%` (4회), `늘봄신용 SE-6 V1 기본 연금리 8.1%` (4회), `누리은행 기관코드 NR-21 식별` (3회)이다.

즉 침입 문서와 누락 문서는 **같은 종류의 사실을 같은 문장 형태로 말하고, 대상 식별자와 시점만 다르다.** 단일 벡터 cosine 유사도는 이 차이를 순위로 분리하지 못했다.

이 실패 지점은 [품질 평가 계획](quality-evaluation-plan.md) 5절이 이미 `높은 vector 유사도만으로 VERIFIED/CONTRADICTED 금지; Structured Lookup·대상·날짜 확인`으로 규정한 위험과 같다. 결과를 본 뒤 새로 만든 해석이 아니라 사전에 기록된 위험이 실측으로 확인된 것이다.

## 4. 위험 핵심 누락

위험 핵심 Recall@5는 `28/30`이다. 누락은 질문 2건이지만 실제로는 **문서 1개** `u-a4aa02efdd355541`을 `channel` 가족의 두 질문(`q-b79d9720777069a2`, `q-a18e80c5f67cb6c0`)이 함께 놓친 것이다.

누락된 핵심 문서는 `문자 링크를 바로 누르지 말고 기관 공식 사이트를 별도로 찾도록 안내한다`는 경보 안내형 문장이다. 대신 들어온 hard negative는 `봄빛문화 BB-C16의 공연 예매 링크는 대출기관의 공식 신청 주소가 아니다`로, 질의의 표면 어휘인 `링크`·`공식`·`주소`·`대출기관`을 더 많이 공유한다.

첫 질문의 4위 hard negative 점수는 `0.5706`, 5위 정답은 `0.5673`으로 마진이 `0.0033`이다. 이 가족의 실패는 큰 의미 오류가 아니라 근소한 순위 역전이다.

위험 핵심 기준은 `1.00`이므로 이 한 문서만으로 blocker가 해제되지 않는다. 이 기준을 완화하지 않는다.

## 5. 사전등록 조건 대비 남은 격차

| 조건 | 기준 | 실측 | 격차 |
|---|---|---|---|
| Recall@5 | ≥0.90 | 0.888 | 정답 칸 +6 |
| Precision@5 | ≥0.80 | 0.728 | 정답 칸 +36 |
| 위험 핵심 Recall@5 | =1.00 | 0.9333 | 문서 1개 |
| slice Recall ≥0.90 · 가족 Precision ≥0.80 | 전체 충족 | 30개 slice 중 21개 위반 | 통과 가족 6/20 |

침입 136칸 중 36칸을 정답으로 바꿔야 Precision 선을 넘는다. 다중 항목 침입이 56칸이므로 **설계된 hard negative 침입의 약 3분의 2를 제거해야 한다.**

Provider HTTP P95와 비용은 이 실행에서 조기 종료로 로그가 누락돼 알 수 없다. 추정하지 않는다. 해당 유실은 PR #35에서 수정했고 오프라인으로만 검증했으며, 수정 이후 v2로 Live를 다시 실행하지 않았다.

## 6. 확인된 사실과 아직 가설인 것

확인된 사실:

- 재계산 수치가 원장과 일치한다.
- 단일 사실 질문 Recall은 1.000이다.
- 다중 항목 침입 56칸 중 53칸이 같은 가족의 선언된 hard negative다.
- 다른 가족 문서 침입은 500칸 중 3칸이다.
- 위험 핵심 누락은 문서 1개에서 발생했고 순위 마진이 0.0033까지 좁다.
- 다중 항목 slice에서 Precision@5와 Recall@5는 같은 값이다.

개발용 split 측정으로 추가 확인된 사실은 7절에 있다. 누락된 정답 문서의 순위와 후보 풀 확대의 회복 가능성은 더 이상 가설이 아니다.

아직 가설이며 확인하지 않은 것:

- 복합 질의를 사실 단위로 나누면 다중 항목 Recall@5가 오르는지.
- 대상 식별자와 시점을 쓰는 Metadata Filter가 실제로 hard negative만 제거하고 정답을 남기는지. 후보 풀 안에 정답이 모두 있다는 사실은 필요조건이지 충분조건이 아니다.
- 개발용 4가족에서 관측한 순위 분포가 새 시나리오 가족에서도 유지되는지.

위 가설도 개발용 데이터에서만 검증한다. Gate holdout으로 검증하지 않는다.

## 7. 개발용 split 순위 측정

6절의 첫 번째 가설인 `누락된 정답 문서의 실제 순위`는 개발용 split에서 직접 측정한다. 이 값이 다음 판단을 가른다.

- 누락 문서가 후보 풀 안쪽에 있으면 `AI-007`의 Metadata Filter와 Authority/Freshness Rerank 단계로 회복할 수 있다.
- 후보 풀 밖이면 표현 자체의 한계이므로 Provider·차원 변경 검토가 필요하다.

측정 도구는 `.github/scripts/provider-embed-development-probe.mjs`이고 `Provider Embedding Development Probe` workflow로 main에서 실행한다.

| 항목 | 값 |
|---|---|
| 채점 대상 | 개발용 4가족 20질문만. Gate 질문은 Provider로 전송하지 않는다 |
| 검색 corpus | Gate와 같은 전체 168 passage |
| 기록 | 질문별 모든 관련 unit과 hard-negative unit의 전체 순위, 점수 |
| 파생값 | `k = 1, 2, 3, 5, 7, 10, 20, 50`의 Recall@k, 질문별 모든 정답을 담는 최소 k, 정답보다 위의 hard negative 수 |
| 계약 | Gate와 동일. `embed-v4.0`, 1024차원, float, cosine, Exact KNN, 재시도 없음, 요청 시작 간격 1,100ms |
| Provider 요청 | 문서 batch 2회 + 질의 20회 = 22회 |

경계는 다음과 같다. 이 실행은 Gate holdout을 소비하지 않도록 `provider-embed-evidence.yml`과 다른 workflow를 쓴다. 결과는 `development-probe-not-adoption-evidence`로 분류하며 `PASS` 판정 필드를 만들지 않고 `evidence/results/`와 Gate index에 쓰지 않는다. 채점 대상에 Gate 질문이 섞이면 실패로 종료한다. pin된 harness 파일은 수정하지 않고 export된 함수만 재사용한다.

개발용 20질문은 4가족의 변형이므로 독립 사건 20개가 아니다. 이 측정으로 서비스 정확도나 Gate 통과 가능성을 발표하지 않는다.

### 7.1 측정 결과

main `2ec62abcb32322e6956e53efdfee62f1f712bb7c`에서 run `33877345188`, job `101037375335`으로 실행했다. 기록은 `evidence/diagnostics/B-EMBED-01/development-probe-33877345188.json`에 있다.

| k | Recall@k | 모든 정답을 회복한 질문 |
|---:|---:|---:|
| 1 | 0.34 | 4/20 |
| 3 | 0.61 | 4/20 |
| 5 | 0.86 | 7/20 |
| 7 | 0.98 | 18/20 |
| 10 | 1.00 | 20/20 |

- 질문별 모든 정답을 담는 최소 k는 **최대 8**, 중앙값 6, P95 8이다. 168 unit corpus에서 모든 정답이 상위 8위 안에 있었다.
- 가장 아래 정답보다 위에 있는 hard negative는 최대 2개이고 13질문에서 1개 이상이었다. 정답을 밀어낸 것은 corpus 잡음이 아니라 그 질문의 지정 hard negative뿐이다.
- 위험 핵심 unit 5개는 모두 5위 안에 있었다.
- Provider HTTP P50 128ms, P95 142ms. 청구 입력 token 6,068, 고정 단가 환산 USD 0.00072816. 이 값은 개발용 22회 요청의 관측이며 Gate 합격 판정이 아니다.

개발용 Recall@5 0.86은 Gate 다중 항목 slice의 0.86과 같다. 개발용 split이 Gate 실패를 대표한다는 근거다.

### 7.2 확인된 결론

관측된 실패는 **표현의 한계가 아니라 top-k 폭의 문제**다. 임베딩은 모든 정답을 상위 8위 안에 넣지만, 다중 항목 질문은 정답 5개를 5칸에 담아야 하므로 hard negative 1~2개가 같은 칸을 차지하는 순간 회복 경로가 없다.

따라서 `embed-v4.0`이나 1024차원을 바꿔야 한다는 근거는 이 측정에 없다. 후보 풀을 10 정도로 넓히면 정답 누락은 0이 되고, 남는 일은 `AI-006`·`AI-007`이 이미 배정한 대상 식별자·시점 기준의 Metadata Filter와 Authority/Freshness Rerank가 hard negative를 제거하는 것이다.

이 결론은 Gate 합격이 아니다. 실제 Filter·Rerank가 정답을 남기고 hard negative만 제거하는지는 아직 측정하지 않았다.

## 8. 재정의 방향

7.2의 결론에 따라 `B-EMBED-01`을 `AI-007` 파이프라인에 맞춰 재정의하기로 했다. Vector 단계는 1차 후보 생성으로 두고 Metadata Filter→Keyword→Vector→Rerank 전체를 별도 component blocker로 평가한다.

이 재정의는 측정 결과를 본 뒤 수용식을 바꾸는 것이다. ADR 본문 변경이 `B-MODEL-01`의 기존 PASS를 무효화하므로 재시험과 재채택이 필요하다. 영향 분석과 단계별 순서는 [재정의 영향 분석과 순서](embed-blocker-redefinition-plan.md)에 있다.

## 9. 후속 작업에서 지킬 것

- 같은 v2 holdout을 다시 실행하지 않는다. 이미 노출된 회귀셋이다.
- 라벨·질문·합격선을 결과에 맞춰 바꾸지 않는다. 불리한 질문을 빼지 않는다.
- Hybrid·질의 분해·구조화 조회 결과를 raw embedding의 `B-EMBED-01` PASS로 대체하지 않는다.
- Provider·차원·검색 계약을 바꾸려면 ADR 변경과 Evidence 영향 검토를 먼저 한다.
- 새 성능 선택은 새 시나리오 가족의 미측정 평가셋과 수용식을 사전등록한 뒤에만 측정한다.
- 이 분석은 `B-EMBED-01`을 `NOT-EVALUATED`로, Implementation Gate를 `NO-GO`로 유지한다.

## v3 실행 (2026-09-05, run `33954521524`)

재정의한 `B-EMBED-01`을 v3 평가셋으로 측정했고 미달했다. 결과를 먼저 적는다.

| 항목 | 기준 | 실측 |
|---|---|---|
| 관련 unit Recall@20 | `= 1.00` | `0.902` |
| 위험 핵심 unit Recall@20 | `= 1.00` | `1.00` |
| 전량 회수 질문 | 100 | 69 |
| Query Provider P95 | `≤ 1,500ms` | `113ms` |
| 비용 | — | USD 0.0012 |

slice로 나누면 원인이 분명하다. 단일 근거 질문 20개는 Recall 1.00이고 모든 정답을
담는 데 필요한 최대 k가 4였다. 다중 항목 질문 80개가 0.877이다. 후보 풀 2,000칸 중
같은 가족 문서는 564칸(28%)이고 나머지는 다른 가족의 비슷한 한국어 금융 문장이
차지했다. 놓친 49칸은 `송금 후 회수`, `이자 절감`, `정책금융 가점`, `공식 확인 경로`
처럼 질문의 중심 주제에서 먼 주변부 facet에 몰렸다.

### 이 실행은 사전등록한 구성이 아니다

`retrieval-blocker-preregistration.md` 3절은 Vector 단계의 입력을 `Filter를 통과한
문서`로 정하고, 5.2·5.3은 문서에 `institution_code`·`product_code`·`effective_from`·
`effective_to`·`authority_level`·`source_fingerprint`·`channel_type`을, 질문에
`target_institution_code`·`target_product_code`·`as_of_date`를 두도록 정했다.

v3 fixture에는 이 필드가 하나도 없었다. v2 스키마를 그대로 따랐기 때문이다. 그래서
harness가 Metadata Filter 없이 전체 168 unit에 대고 벡터 검색을 돌렸다. 제품이 쓰지
않을 구성이다.

따라서 `B-EMBED-01`은 `FAIL`이 아니라 `NOT-EVALUATED`를 유지한다. 사전등록한 구성을
재지 않았으므로 Provider의 실패로 귀속할 수 없다.

### 그래도 남는 관측

이 측정은 `필터 없는 전체 corpus 벡터 검색` 기준선으로는 유효하다. `AI-006`이
`Vector 유사도만으로 사실을 확정하지 않는다`고 정한 이유를 숫자로 뒷받침한다.
주제가 균질한 corpus에서 다중 항목 질문의 1차 회수는 임베딩 단독으로 보장되지
않는다.

### 대가와 후속

`assertUnmeasuredGate`가 fixture blob SHA로 재사용을 차단하므로 v3 홀드아웃은
소모됐다. 합격선을 낮추지 않고 v4를 새로 만든다. v4는 사전등록 5.2·5.3 필드를
갖추고 harness가 Filter 단계를 실제로 거친다. v3 fixture와 이 결과는 지우지 않고
이력으로 보존한다.
